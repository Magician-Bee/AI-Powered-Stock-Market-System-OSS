# 自主交易商品身份與新增部位核驗

目標對應 M2／05、07，並補 M1／03、M4／22、M5／23、M7／31 的來源與執行邊界。工作分支為 `codex/fix-product-identity-admission-20260912-2025`，基準為 `88c0069dd94a42506ba9e03efed9a480b2b4625a`。本報告不把工程測試或商品分類當成真模型成交、獲利資格或全球市場完成。

## 實際斷點

既有 TWSE／TPEx 公司及行情主檔合併流程，將未辨識成 ETF 的項目預設為粗粒度 `stock`。已保存的正式掃描因而把 `020000.TW` ETN 與 `9103.TW` 存託憑證留在普通股候選範圍。只改新的下載器不能修正存量主檔；只改研究名單也不能阻止舊研究或舊等待計畫沿用錯誤身份。

另有兩個可重現問題：本地主檔空值或驗證錯誤會讓 GET 意外退回網路下載／寫入；UI 將有效數量 0 以 `or`／`||` 替換成整個研究名單數量。

## 官方證據及保存方式

來源採[證交所證券編碼公告](https://www.twse.com.tw/zh/products/code/announcement.html)所連結的上市、上櫃、興櫃 ISIN 完整名錄，核對官方類別、CFI、完整代碼、ISIN 與市場欄。保留普通股、ETF、ETN、存託憑證、特別股、權證與其他／未知商品；創新板及興櫃另保留交易板別。分類規則由解析與准入共用，不用名稱或代碼前綴取得普通股資格。

來源登記為 `twse_isin` 三個 HTML 資料集，各次至多一個 20 秒請求及 16 MiB 回應。沿用既有增量檢查點、更新鎖及傳輸管制，保存原始位元組後才解析；錯誤、轉址、截斷與格式異常也留存取得證據。來源日期與接收時間分列，這份目前名錄不取得歷史 PIT 資格。

分類依 `venue:symbol` 寫入既有 entity metadata 的獨立欄位。更新不重建 entity、不按公司統編合併股別、不改生命週期，也不改寫歷史 AE 或計畫。完整成功名錄中缺少的舊項目改為 unknown 並留缺席來源；失敗或空來源不代表全市場下市。舊刷新不得覆蓋較新收據。

本地解析器須從保留的原始資料回讀核對完整分類，而非只相信 metadata 的標籤。缺原文、雜湊／日期／代碼／來源／CFI 不符或身分歧義都回 unknown。原文解析結果最多快取三份；資料庫變更後重新核對位元組，持續查詢不重複解析整份名錄。這是 Host 保存與核驗證據，不是獨立第三方證明。

## 研究、計畫及委託

研究範圍保留未知與非普通股；來源支援時仍可保留日線研究。只有目前支援的普通板普通股會套用現有股票成本產生固定候選，新提案也須通過最新 Host 商品身份核驗。研究代碼支援正式四至六碼英數標記，送單仍維持原本已支援的數字代碼及交易規則。

新計畫保存商品核驗收據；送單前核對最新身分、原研究 entity、活動狀態、普通板及普通股。來源及接收日期不得來自未來、不得超過七個曆日，來源日期也不得晚於接收日期。這是新增的商品證據時效規則，不變更原帳戶資金、部位、損失或實驗資格。

既有買單先核對回報。若商品資格失效，保存同單撤銷意圖並取消尚未成交買量，重啟後仍繼續確認；已成交部位依原計畫管理，不因分類查詢故障自行全部賣出。既有核對、撤單與賣出不以分類來源恢復為前提。

本地主檔及 UI GET 不發起來源更新。明示自主研究／既有盤後研究流程會先依檢查點刷新主檔，再掃描；來源失敗保留狀態與原文引用。研究執行於 worker，不阻擋委託管理。即時研究在掃描完成後固定判定時間，讓掃描期間取得的名錄可按實際可得時間核驗；明示歷史時間不變。取消研究會停止後續來源分區，目前已開始的分區可完成回應保存與檢查點收尾。研究上限、模型次數、步數及工具總輸出預算維持。

API／UI 保留研究總數，另顯示核驗普通股、可新增部位、未知及排除原因；0 保持 0。保存的研究快照在讀取時核驗收據時效，不冒稱每次 GET 都已讀取最新主檔。真正建計畫與送單使用最新解析器。研究摘要保留二十個完整歷史 ID，非普通股候選限制與歷史失敗分開。

前瞻母體改依核驗商品收據選取；來源登記與可得時間規則也納入凍結政策。舊研究／舊試驗不得回填為新版合格資料，120 日觀察、30 筆平倉及原有成本後統計條件不變。

## 驗證紀錄

測試涵蓋官方格式及原文回讀、存量身分保留、舊刷新與缺席名錄、未知／衝突／錯配／過期商品、兩個建計畫入口、送單前分類改變、部分成交撤殘量、回報遺失後重啟、既有出場、空主檔 GET、零值、廣研究契約及批次來源失敗隔離。整合回歸 **1,093 passed、0 failed、0 skipped**（54 個檔案，463.35 秒），僅有既有 Starlette 相依套件棄用警告；Python 編譯、前端 JavaScript 語法及中立性檢查通過。隔離固定行情閉環完成兩筆 PaperBroker 成交、平倉、單一成果及冪等對帳；此結果沒有真模型呼叫，不替代 M1 或市場獲利。

隔離真來源重播已通過：從正式 market DB 的同一唯讀交易複製 55,985 個實體、114,857 個識別碼與 0 筆身分合併；重播三份保留的官方原文，共 47,334 列。更新前後，商品 metadata 以外的身分雜湊完全相同。45,895 個目前精確掛牌身分中，45,254 個分類已核驗、641 個未知，普通股類型 2,324 個，其中 1,936 個通過現有商品門檻。官方名錄仍有 2,071 個項目未匹配存量身分，歧義為 0；這些缺口沒有被刪去或宣稱已完成。

七個來源樣本結果一致：`020000.TW` ETN、`9103.TW` 存託憑證、`00400A.TW` ETF、`2887Z1.TW` 特別股與 `2237.TW` 創新板都不准新增部位；`2330.TW`、`8299.TWO` 通過商品核驗，仍須獨立滿足行情、風控與交易條件。

重播腳本與收據位於本機 ignored `output/product-identity-investigation-20260912/`：`verify_retained_catalogs.py`、`probe-20260912-v1-receipt.json`。收據 SHA-256 為 `f525daeb93209db94f353e45984939a5de4281dbd89f696a8a0dc815584cfd51`。重播沒有正式 SQL 寫入、新 HTTP、模型、計畫或交易；原始取得紀錄缺少的 request-start／HTTP-status，在重播中明示為適配假定，不冒充新的 HTTP 觀測。正式 DB 唯讀連線可能建立 SQLite 協調讀取用 WAL／SHM 輔助檔，沒有修改市場資料。

21:31–21:32 對實際 managed market DB 執行商品專用 loader（`force=false`），三份官方來源各取得一次 HTTP 200。上市 34,918、上櫃 9,987、興櫃 358 個既有掛牌 metadata 更新成功，合計 45,263；45,895 個目前掛牌身分的讀取核驗結果與隔離探測一致（已核驗 45,254、未知 641、現有商品門檻通過 1,936）。寫入筆數與目前讀取核驗分母不同，沒有互相代替。七個樣本全部符合預期。

正式維護前後，商品欄位以外的實體、識別碼、合併及生命週期完整列雜湊不變；歷史 revision 的索引承諾欄位、歷史 AE、計畫、訂單、成交及財務表雜湊不變。原始維護收據的廣義「所有表完全不變」判定為 false：既有背景 `_bind` 同時同步三筆 `autonomous_model_reviews`，讀取時它們的 `updated_at` 均為 21:31:52。未保存更新前各欄位內容，不能僅憑表雜湊宣稱差異只有時間戳。該維護驗收的觀測時間內查無新 run，最新仍是 15:51 的舊 partial run；這個結論不涵蓋後述 21:51 的介面驗收意外任務。

保留廣義失敗收據 `production-refresh-receipt.json`（SHA-256 `0d978338faf85a5445c079102f560e20be6d161118220b6ab5effac013b4cfea`），另有明示範圍的分類驗收 `production-refresh-scoped-acceptance.json`（SHA-256 `bcfe078ad8ad76ccbc969e93f961c666bb351b2e0e4825edcce799e88aff39b0`）。這些為不同資料庫各自的唯讀交易，不宣稱全服務停止寫入或跨庫原子快照。

21:26:51 實際操作自主帳戶刷新成功，按鈕恢復可用；TWD 1,000,000、0 計畫／掛單／持倉／成交，模型維持 `gpt-5.6-sol`／medium、1／1 次及 12 步。提交前部署指紋為 `b651720db0218ad2`，當時 HEAD 仍是 `88c0069d`，不能用舊提交宣稱已包含本輪程式。

首頁實際操作確認舊快照的商品數量 0 不再被研究總數替換。兩次市場掃描遇到既有 TPEx 批次報價 HTTP `IncompleteRead`，每次沿用原有最多三次請求與 transport guard，畫面保留舊成功快照並顯示失敗。這暴露出單一來源失敗會丟棄另一市場成功資料的斷點，因此另補任務 08：按市場完成載入後才使用該份資料，單一來源失敗保留另一成功來源，失敗市場價格及行情日期留空、品質 insufficient、明示 invalid 取得失敗證據；兩個來源皆失敗仍保留舊快照。每個 loader 的重試與傳輸限制不變，未取得行情不會因此成為可執行候選。

21:48:21 新部署後實際 UI 掃描成功，保存 `MIS-84a0a3a433fd473e93a5c523aa1ca12e`。研究 4,606 項，商品核驗 3,839、未知 767、衝突 0，1,897 項通過商品門檻；有效完整資料及模型深入分析仍為 0。這個 5,000 筆主檔讀取上限下的快照分母不同於完整主檔。兩個批次來源本次都成功，不能將它當作真實單邊失敗隔離驗收；失敗隔離由八項隔離測試含保存／回讀驗證。快照中的 `020000.TW` 明確為 ETN、`9103.TW` 為存託憑證，皆保留研究並限制新增部位，模型狀態為未分析。快照 JSON 內容 SHA-256 為 `633d6fe760b8c12480f475fd80c4ba3c3c18c671dd4bda023790b7742fcaaa1a`。

21:51:46 的後續搜尋驗收發生意外：輸入已存在的 `020000.TW` 後，首頁未命中目前渲染名單及實體搜尋，既有 fallback 自動點擊 Agent 傳送。產生 Advisory `AR-ff1fa2f11b9846498d37ac050bb2c084`，於 21:53:18 完成；取消操作未成功，不能記為已取消。實際使用 `gpt-5.6-sol`／medium，完成三個 SDK 模型回合與三個研究工具呼叫，沒有修改原預算。SDK 用量物件為空，tokens／金額未知，不能填 0。

該任務的主檔搜尋明示 `refresh=true`，刷新遇到來源截斷而未進入本地查詢；其後網頁研究及市場分析完成，最後仍限制執行。部署模組與工作區雜湊一致，沒有載錯程式的證據。22:00 的唯讀審核仍為自主計畫 0、M1 false，且尚無更晚的新 run。完整意外紀錄保留於 `search-implicit-advisory-incident.json`，收據 SHA-256 `3ff87e3b78da0232b17bf68b942ba342cf95b94b5eb81b76bb9e3cd3ea8e1838`；它不替代自主研究或交易閉環。

搜尋修正改為：完整台股代碼先唯讀取得完整已保存快照中的單檔明細，核對 symbol／snapshot_id；未命中只放入草稿，須另按傳送才啟動模型。提醒、紙上預覽及選取 context 引用同一份明細的快照；刷新後清除舊搜尋身份並採新明細的 entity。另有 **11 項 JavaScript 行為回歸通過**，與前述整合測試去重共 **1,104 項**。實際 UI 已顯示 ETN 名稱與「研究保留／新進場受限」，未命中查詢顯示草稿提示，後續 runtime 唯讀查無新增 run。

搜尋後的自主帳戶頁曾連續逾時，15 秒期限到達時隱藏舊數值。既有 notification preview 的日報／自選聚合透過 `securities_for_universe` 仍隱含下載整個報價主檔，來源截斷時其他請求也延後；帳戶狀態入口本身沒有做分類或市場下載。來源請求結束後，22:14:15 UI 恢復 TWD 1,000,000、0 計畫／掛單／持倉／成交。補將該 metadata 入口接回本地主檔，新增五個離線回歸，空資料或本地讀取失敗維持 unknown，沒有來源回退。這只移除已證實的隱含全市場主檔下載；日報／通知的行情、新聞及財務請求仍需各自取得資料，不宣稱全部非必要請求都已與管理隔離。

22:15:42 正式帳本唯讀收據再次確認自主現金及已結算現金皆 TWD 1,000,000，計畫、訂單、成交、持倉及成果皆 0，研究循環 5；模型維持原每日 1 次／12 步。意外任務完成後尚無新 run。收據 `post-incident-account.json` 的 SHA-256 為 `355650b0389ff1addb400fccf3ef1e1ce34658ff53782f1cd2b3a2f1f35cb537`。

後續仍觀測到背景通知的行情／新聞請求拖延帳戶頁，故另補 M6／28–29 的有限 UI 修正：通知通道與預覽只在市場監控頁可見時載入；相同標的合併在途請求，換標的、離頁、隱藏頁面及四秒逾時皆使用 AbortController 取消，舊回應不覆蓋新結果。自主帳戶原十五秒期限不變。九項 JavaScript 行為測試通過；三份驗收測試去重合計 **1,113 passed、0 failed、0 skipped**。前端 abort 不代表後端上游作業必定同步停止，其他財務頁的資料請求也未因此全部隔離。

最終待提交部署指紋 `54825e16185d24ec`：實際自主帳戶刷新成功、按鈕恢復可用。開啟市場監控後，通知通道 HTTP 200；預覽逾四秒顯示失敗，瀏覽器記錄同請求 `net::ERR_ABORTED`／cancelled。返回自主帳戶後再次刷新成功，取得 22:32:38 新快照，TWD 1,000,000、0 計畫／掛單／持倉／成交及原 1／1 次／12 步模型設定均可核對。本次有限故障驗證不能延伸成所有資料中斷與長期運行皆已通過。

## 尚未達成

M1 仍需新部署版本的真模型研究、提案、真行情買入、管理、出場與對帳證據；現有零交易不能算完成。商品分類不代表多面向資料已齊全，研究讀取上限與主檔未匹配／歧義項目仍須如實列入覆蓋缺口。ETF、ETN、特別股、存託憑證、創新板及興櫃沒有因此取得現有股票委託規則的執行資格。美股及其他市場、正式券商驗收與實盤授權仍未完成。
