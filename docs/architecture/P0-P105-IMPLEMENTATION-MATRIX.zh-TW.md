# P0–P105 實作、測試與驗收追溯矩陣

本文件依據 P0–P105 最終規格與目前 shared working tree（更新至 2026-08-13）逐項核對。它是「程式落點與待驗收事項」清單，不是完成宣告，也不取代測試執行紀錄。

2026-09-08 推進 P73 首頁市場清單：全市場 `watch`／`avoid` 等大型清單不再一次建立數百張候選卡；原生 UI 首批只渲染 40 張，明示「已顯示／總數」並由使用者逐批載入。切換分類會重設視窗，少於 40 張的分類不顯示多餘控制；股票名稱可保留兩行，代號、名稱、分類與成交量都有完整 title，避免窄欄位互相擠壓。`.command` managed runtime 在 1,440px 寬原生視窗驗證 `40 / 648 → 80 / 648`、切換 `34 / 34` 後回到 `40 / 648`，未操作 Run 或呼叫模型；證據見 `docs/validation/native-p73-market-list-window-20260908.md`。

2026-09-08 完成 SEC-009：Broker OMS 的 Host 風控允許與拒絕現在會在 broker I/O 前寫入與委託狀態相同的 durable、append-only、content-addressed retention authority；含敏感欄位的 audit details 會 fail closed。`Hosted critical retention archive` run `34224155552` 對 candidate `b81c6228261d7183502d0fe1155e98f017883720` 以正式 OMS writer 建立 sandbox 委託與兩筆安全決策，再由第二台乾淨 GitHub runner 下載 90 天 off-host artifact、還原至全新 SQLite 並只依 archive 重建 `CREATED → SUBMITTING → ACKNOWLEDGED → FILLED`。重建收據逐筆綁定 record/content hash、原始 intent hash、安全決策與 archive hash；獨立 verifier 與 `PRAGMA quick_check=ok` 均通過。完整 artifact 雜湊、原生桌面驗收與未使用模型的證據見 `docs/validation/hosted-sec009-offhost-audit-20260908.md`。

2026-09-08 完成 A-011：`Hosted Session archive restore` run `34222048542` 在 candidate `3271691b4a67dff2b965938fcb10510385d18bee` 建立 secret-minimized、content-addressed Session archive，經 90 天 off-host artifact 交給第二台乾淨 GitHub runner。乾淨 runner 將完整 Session、title history、兩則 message、完成資料夾案索引及含 events／evidence／artifacts／versions 的不可變 envelope 寫入全新 SQLite，讀回內容與原 archive 完全一致，`PRAGMA quick_check=ok`，重複還原拒絕覆寫。下載後獨立稽核確認測試密鑰未洩漏、1 Session／1 資料夾案／2 message／1 envelope 均存在且 gate hash 通過。完整雜湊與原生桌面驗收見 `docs/validation/hosted-a011-session-archive-restore-20260908.md`。

2026-09-08 再推進 GOV-005（維持 partial）：公司行動帳務套用、借券費扣款、T+2 結算、settlement P&L、broker order reconciliation，以及 broker account reconciliation 的結果與觸發，現在都由正式 writer 寫入與業務資料相同的 durable SQLite retention authority；錯誤資料庫或缺少必要 retention authority 時會 fail closed。GitHub Actions run `34220764365` 在 candidate `71b0527bddf19a30b7b10e0cec1d4b96332ed6f2` 實際呼叫上述 writer，加上既有 Paper OMS、Paper Broker 與 Broker OMS，共匯出 10 種 critical schema；第二台乾淨 runner 還原 10 筆且 `PRAGMA quick_check=ok`。兩份 artifact 保留至 2026-12-07，仍不足以證明 production-years retention，因此 GOV-005 唯一 blocker 收斂為 `production_years_archive_retention_is_not_yet_provisioned`。完整雜湊與原生桌面驗收見 `docs/validation/hosted-gov005-critical-writers-20260908.md`。

2026-09-08 推進 GOV-005（維持 partial）：`Hosted critical retention archive` run `34141401316` 在 exact candidate `9198eb6e51ed6d2ad58e60ef4406a0c2cfad9384` 將 Paper OMS、Paper Broker 與 Broker OMS 三種 execution-critical snapshot 匯出為 content-addressed archive；一般 signal 在維護後只保留 bounded projection，不進入 critical archive。第二台乾淨 GitHub runner 重新驗證 archive、record payload、retention receipt 與所有 SHA-256，寫入全新 SQLite，`PRAGMA quick_check=ok` 且三筆 critical rows 可逐筆重建。兩份 off-host artifacts 保留至 2026-12-06；gate 明確記錄 `scope=hosted_drill_only` 與 `production_years_satisfied=false`，不把 90 天 artifact 冒充多年 production archive。尚需多年不可變離站存放與剩餘 execution-critical writer 完整接線，故 GOV-005 不改為 complete。完整雜湊與原生桌面驗收見 `docs/validation/hosted-gov005-critical-retention-20260908.md`。

2026-09-07 完成 SEC-005／SEC-006：`Signed release SBOM and provenance` workflow 對 exact candidate 建立可重現 source archive、由釘定 `uv.lock` 產生 CycloneDX SBOM，並產生綁定 commit、artifact SHA-256 與 signer identity 的 provenance。由於 GitHub Artifact Attestations 不支援 user-owned private repository，第一次 run `34118886214` 的拒絕沒有被算成成功；正式路徑改用 Sigstore cosign v3.0.5，透過 GitHub Actions OIDC／Fulcio keyless identity 分別簽署 source、provenance 與 SBOM，三筆簽章均寫入 Rekor。成功 run `34137501131` 對 candidate `fe41ed5a59427a9c389a32b025b3f7f029bf1c71` 完成 `build-and-sign` 與獨立 `clean-runner-verify`；後者重建 SBOM／provenance、核對相對路徑 checksum，再以固定 workflow identity 與 issuer 驗證三份 Sigstore bundle。兩份 artifacts 保存 90 天，下載後 gate receipt 為 `passed=true` 且無 blocker。完整雜湊、Rekor index 與原生桌面驗收見 `docs/validation/hosted-sec005-sec006-release-attestation-20260907.md`。

2026-09-07 完成 R-004：新增 fail-closed OTLP/HTTP JSON exporter，只有 HTTPS 外部端點或 loopback HTTP 可用，拒絕 userinfo、query／fragment 與非標準 traces path。`Hosted OpenTelemetry tracing` 每週排程並可手動演練；第一台 GitHub-hosted runner 啟動固定版本 `otel/opentelemetry-collector-contrib:0.160.0`，實際接收並由 file exporter 持久化同一條 request → decision → paper-order trace，三段資源分別為 `stock-ai-api`、`stock-ai-decision`、`stock-ai-order`。第二台乾淨 runner 下載 90 天 artifact，重新綁定 candidate、Collector image/config、OTLP payload/delivery receipt、trace/span IDs 與 parent chain。GitHub run `34117518277`、candidate `e6b76f12ac36acd518318cc6fd60f1d142c4dbee`、兩個 jobs 與兩份 artifacts 已通過；下載後獨立驗證 gate `passed=true`、無 blocker。完整雜湊與原生桌面驗收見 `docs/validation/hosted-r004-otel-tracing-20260907.md`。

2026-09-07 完成 R-003：`Hosted migration retention` 每週排程並可手動演練，在第一台 GitHub-hosted Linux runner 對真實 legacy SQLite 執行 startup migration：先建立 hash-verified pre-migration backup、驗證連續 v1–v47 manifest、提交 migration 並保留既有資料。清理先產生 hash-bound dry-run，再只刪除完全相同的兩組過期 completed pair；最新兩組、prepared forensic pair 與未知檔案均保留。保留的三組 pair 以 90 天 artifact 離站保存，第二台乾淨 runner 重新下載並逐組驗證 receipt、bytes、schema、integrity 與 legacy content。GitHub run `34115642136`、candidate `0a98652eed52632c3ebe54e1c7b4a5b8d0edd654` 與 artifacts 已回下載獨立驗證；完整雜湊與原生桌面驗收見 `docs/validation/hosted-r003-migration-retention-20260907.md`。

2026-09-07 完成 R-002：`Hosted SQLite backup restore` 每日排程並可手動演練，以兩個獨立 GitHub-hosted Linux runner 執行。`create-backup` 使用 SQLite online backup API 產生一致快照，綁定 backup bytes、schema、integrity、內容與政策 SHA-256，並以 30 天保留的 GitHub Actions artifact 作離站儲存；`clean-runner-restore` 只在前一工作完成後於全新 runner 下載，對不存在的目標執行還原，逐列比對 source manifest、通過 `PRAGMA quick_check`，並實證既有目標拒絕覆寫。首次回下載稽核發現 artifact 含不需要的 SQLite sidecar，最終 candidate 改以白名單只保存四個備份輸入與兩個還原證據檔。GitHub run `34114119395`、candidate `0a0245095d59c61cb81f94e44f93e783e523e8e3` 與兩份保留至 2026-10-07 的 artifacts 已重新下載並獨立驗證；完整雜湊與原生桌面驗收見 `docs/validation/hosted-r002-backup-restore-20260907.md`。

2026-09-07 完成 D-012：指定桌面 launcher 現在明確啟用 Host-owned macOS Notification Center sender。資料平台按「評估並通知」後，會從不可變來源狀態重新產生 SLO／延遲／缺分區／revision 告警，逐筆送到登入中的本機通知中心，並把 OS request acceptance、訊息 SHA-256 與 `os_request_accepted_only` 語意寫入既有 append-only delivery ledger；通知只含來源、資料集、嚴重度與告警碼，不含 details 或秘密。UI 同時顯示 delivered／failed／not configured 數量及收據 hash。本機投遞不代表 Email、Slack、LINE 或遠端 on-call 已配置，R-006 仍維持 partial。

2026-09-01 修正 P51/P87 Critic receipt 投影與重複子分支：原生驗收發現資料庫中的 Critic child Run 已 `completed` 並完成 Join，但父 Run 走「資料不足」安全收束時沒有把該 receipt 投影到最後摘要；另若將 generic `agent.run_subtasks` 暴露給 remote provider，模型可能自行拆出多個非 Critic 子任務。現在 Critic 只有在 child result 實際為 `completed` 時才被視為完成；市場證據 validator、重複 delegation 防線與兩種 Host 摘要共用此 receipt 規則。明確要求的 Critic capability 保留在 Host execution manifest，從 provider disclosure 移除，由 Host coverage protocol 僅建立一個 `role=critic` child，不讓模型自行展開。回歸 256 passed；以 `開啟股市AI系統.command`、遠端 `gpt-oss:20b` 及 Computer Use 實測 Run `AR-6f58b02e9a2e414e897064ad8a125c5a`，父 Run 與唯一 child `AR-e0bef3d3134443f2a53bdceaeda655e7` 均 `completed`，tool receipt 只有一個 `agent.run_subtasks`、`role=critic`、`count=1`，桌面最終摘要顯示「已完成並 Join 反方子分支」。本次只做研究，未建立紙上單、實盤或自動化。這修正的是 P51/P87 Host receipt/branch authority，不把上游資料不足誤報為完整市場研究。

2026-09-01 修正 P1/P40/P78/P82 的通用工具參數迴圈與桌面驗收來源：先前 Host 只會在工具 arguments 不是 JSON 時攔截；若遠端 provider 送出格式正確、但含未知欄位、漏填必填欄位或超出範圍的 JSON，該呼叫仍會進入 durable PlanGraph，反覆產生 `invalid_tool_arguments`／`plan.compile.failed`，最後才耗盡 no-progress guard。現在每個已揭露能力在進入計畫前都依註冊 input schema 驗證；不合法呼叫會保留欄位路徑與原因、交由 bounded repair 或回饋下一回合，絕不寫入 executable plan。`開啟股市AI系統.command` 同時改為使用與正式 launcher 相同的 Application Support runtime identity，避免只清除舊 `.runtime` 路徑而讓原生 WebKit 視窗連到舊服務。回歸 `337 passed`，其中端對端測試證明 `$.limit` 類未知欄位被拒絕後，修正後呼叫可完成且沒有 `plan.compile.failed`；Computer Use 實際以遠端 `gpt-oss:20b` 執行 2887.TW 基本面／技術面／風險研究，Run `AR-2260b9ae4eb64fa486e20206f3770b7a` 正常 `run.completed`，沒有紙上／實盤／自動化。分支 `codex/fix-agent-schema-launcher-20260901-2254` 已合併至 main commit `2adad065107de5ad18ec0da16496155d9d9db13f`；這完成本機避免無效工具計畫的 Host 邊界，真實 fault injection、跨 provider 與外部 execution evidence 仍各自維持需驗收。

2026-09-01 推進 A-005（維持 partial）：Host 現在在每個含外部市場／網頁證據的 provider turn 前，重新驗證每個 `open_stock_ai.untrusted_content.v1` envelope，並建立 `open_stock_ai.provider_untrusted_context_receipt.v1`。此 durable receipt 只保存 data-only policy SHA-256、已驗證外部內容 SHA-256 清單與內容數量，絕不複製新聞文字、URL、使用者文字或任何憑證；收據遭改寫即無法驗證。Runtime timeline 會顯示「外部資料已以 data-only 邊界送入模型」，供操作員從原生 UI 辨識此 Host 界線。測試同時驗證 hostile text 不會進入 receipt，且整個兩回合 Runtime 在市場觀測後才建立有效 receipt。A-005 尚須以實際 remote provider 的後續 turn 留下 receipt，並完成獨立 hostile corpus review sign-off，故不得標示 complete。

2026-09-01 修正批准恢復與紙上 lane 的兩個 Host state-machine 缺口：本機 `artifact.create_text` 一旦由使用者批准，resume 會先執行同一個 plan node 與完全相同的 arguments，不再重新詢問模型、產生不同 Artifact 或第二張批准卡。另將「不建立 Artifact、紙上交易、實盤交易或自動化」這類以標點並列的否定語意納入 completion contract；它不會再被誤判為紙上交易請求或觸發 paper preview／submit／自動 continuation。回歸測試覆蓋 approved resume 不呼叫 provider 與標點分隔的否定條件；原生 UI 後續驗收仍使用 `開啟股市AI系統.command` 與遠端 `gpt-oss:20b`。

2026-09-01 修正公開研究在工具執行前卡住的 Host contract：內建 market coverage planner 曾對 `web.research` 送出 `limit`，但同一工具 schema 只接受 `source_count`；遠端 OpenAI-compatible provider 也會自然地產生 `limit`，於是 PlanCompiler 反覆回報 `invalid_tool_arguments: $.limit`、沒有任何 tool dispatch，最後才耗盡 no-progress guard。現在 coverage planner 使用 `source_count`，而工具 schema 將受限 `limit` 作為相容 alias；所有值仍限制為 1–5。桌面原生 Computer Use 以一般使用者研究句建立 `AR-429ac23d20634c0aac2cd1d049c76907`，顯示 `advisory`，完成 `market.analyze_symbol` 與 5 個來源的 `web.research`，sequence 71 正常完成；沒有 Artifact、紙上單、實盤或 Automation。詳細證據見 `docs/validation/native-advisory-research-20260901.md`。

2026-09-01 修正紙上任務最終摘要遺漏分析範圍：先前使用者以自然語言同時要求基本面、技術面、風險與一筆紙上模擬交易時，Host 雖已完成並持久化所有已取得的分析收據，最終訊息卻只顯示紙上買進完成，讓前半段需求在 UI 中看似消失。現在 `paper.submit_order` 的 Host 終態會保留已驗證的基本面（月營收）、技術面（行情／訊號）與風險閘門摘要；缺少的維度明確標記為未取得證據，資料 blocked 時也保留限制。摘要不重用模型自由敘述、也不把紙上成交變成投資建議或實盤授權。以桌面 `開啟股市AI系統.command` 和遠端 `gpt-oss:20b` 實測「分析台新新光金（2887.TW）的基本面、技術面與主要風險，然後完成一筆紙上模擬交易」：Host 自動選擇 `paper_execute`，5/5 完成 `market.analyze_symbol`、`market.monthly_revenue`、preview、submit 與 final，沒有要求使用者提供操作指令、外部驗收或券商設定，且沒有送往實盤券商。

2026-08-31 修正 P1/P12/P40 的自然語言紙上任務收束：使用者只需說「分析這檔股票，做一筆紙上模擬交易」，不必補寫「不要等待外部驗收」、買賣方向、數量或如何回答。若目標或目前畫面只確認一個標的，Host 固定建立 1 股零股買進的本機沙盒 preview／submit，並在 provider 嘗試以「無法決定參數」、「等待外部驗收」或其他非批准型追問停住時，自行繼續同一個 Host 驗證流程；任何實盤、帳戶、外部傳送與 `waiting_approval` 仍不受此規則放寬。Activity timeline 也不再顯示模型未驗證的數量敘述，而只顯示 Host 的 1 股預設或實際 receipt。桌面 `開啟股市AI系統.command` 以遠端 `gpt-oss:20b` 原生 UI 驗收：2887.TW Run 完成 market analysis、paper preview、paper submit，4/4 steps completed，無 `waiting_user_input`／外部驗收卡；SQLite tool receipt 確認 preview 與 submit 完全相同，均為 1 股 odd-lot、local-paper-only。這是 Host completion／execution policy 修復，不是用提示詞教使用者如何提問。

2026-08-31 完成 N-006：桌面 `.command` 啟動時會把 project runtime 移到 project-scoped macOS Application Support（桌面路徑只保留相容 symlink），並讓 launchd 從受管 source snapshot 與受管 n8n runtime 啟動；因此 Finder／終端啟動器結束後，Python UI service 與 n8n 不再因 Desktop privacy domain 被回收。n8n callback graph 現在把已 HMAC 簽署的 JSON 字串以 raw `application/json` 原樣送到 Host，避免 HTTP node 重新序列化造成驗簽失敗。實機建立無下單、無外送、只記錄 App 內結果的 Taiwan calendar `run_once` cron Automation，重啟本專案 n8n 後取得兩筆不同 SHA-256 的 authenticated Host callback receipts，第一筆完成 Host reanalysis；驗收 workflow 隨後在 Host 與 n8n 都封存／停用。N-006 因此標為 complete；這不解除任何實盤、帳戶或外部通知權限邊界。

2026-08-31 完成 N-005：本機 project-owned n8n gateway key 現在除了最小 workflow lifecycle scope，也必須具 `execution:list/read/delete` scope；舊 key 缺此範圍時 bootstrap 會只替換同標籤的本機 key，不會接觸 callback secret 或其他憑證。新增 `maintain-n8n-executions.py`，唯讀預檢會以 n8n 公開 API 與本機唯讀 SQLite projection 交叉比對，僅允許清理「已封存且名稱為 Stock AI Automation」的 execution；API inventory 與資料庫不一致即 fail closed，刪除一律走 n8n 官方 `DELETE /api/v1/executions/{id}`。本次實機清理 12 個已封存工作流中的 59 筆 error execution（477,667 bytes、59 筆敏感欄位標記），後驗證為 0 筆／0 bytes／0 標記；收據只保存計數、hash 與動作結果，且在 ignored runtime 目錄以 0600 權限保存。停止本專案 n8n 後的 SQLite offline compact 亦通過 `quick_check`，資料庫由 46,657,536 bytes 壓實至 2,015,232 bytes，再由 `開啟股市AI系統.command` 正常重啟原生 UI 與 n8n。N-005 因此標為 complete；N-006 仍需獨立的實際 schedule restart／signed callback receipt。

2026-08-31 修正 P1/P40/P78/P82/P88/P93 的自然語言研究收束：原生 Agent Dock 實測顯示，`market.research_pack` 對 TSM 失敗後，成功的同標的 `market.analyze_symbol` 雖有結構化 `symbol`，卻沒有被 Recovery scope 視為同一範圍，令失敗 Branch 永遠未修復；另外 `token_budget_exhausted` 與 execution guard 未被視為和 cost budget 一樣的終止邊界，造成畫面出現開始／暫停／開始的自動續跑。Host 現在以工具 arguments／validated result 中的 `symbol` 建立同標的 recovery link，並把 token／cost／guard 都設為不可自動續跑的資源邊界。一般「分析技術面與風險」本身也預設為分析模式；資料不足時 Host 直接以來源、時間與限制輸出安全結論，不要求使用者額外輸入「只做分析」。最終答案驗證同時拒絕 `Requesting…`／`請提供…` 等進度文字冒充完成。以本專案 `開啟股市AI系統.command` 重新載入桌面原生 App 後，遠端 `gpt-oss:20b` 的 TSM 分析 Run 顯示 Host 可讀摘要並完成，沒有 `continuation_requested`、沒有 Automation、紙上訂單或實盤券商操作；本次相關套件為 242 passed。這是 Host state machine／completion contract 修復，不是調整模型提示詞；其他真實資料品質與多 provider 仍依矩陣維持待驗收。

2026-08-29 修正 P5 child Branch transition authority：先前 child Run status、Branch control 與 Local Plan／Join 分別由不同 helper 直接更新 SQLite；延遲 callback 因而可能把已完成 child 改回 running，令 parent Join 與 Dock 出現不一致。現已統一到 `DurableForestStore.transition_branch`：每次轉換原子寫入 Branch、Local Plan、Join 與 provenance，並拒絕 terminal Branch 的 reopen callback。這是 Host state machine 修復，不依賴模型或使用者提示；`test_agent_forest_projection.py` 已驗證 completed child 的延遲 running callback 被拒絕，兩個 child 完成後 Join 仍 durable completed。能力 executor 的 in-memory scheduling snapshot 目前仍只作 checkpoint transport，故全 Run 唯一 execution authority 與多 child 的原生並行觀察仍持續驗收。

2026-08-29 修正 P1 紙上任務誤拒絕與完成迴圈：`ProposalArbitrator` 先前無法把「不得送出真實交易」辨識為對 real-trade marker 的否定，因此同一個 objective 合法要求「1 股零股紙上模擬買進」時會錯誤走到 `proposal_rejected`、不建立 Run。現已把「不得」納入 bounded negation parsing；命名券商／真實帳戶的實盤送單仍維持拒絕。第二層根因是紙上 lane 的 capability filter、Host 自動唯讀證據步驟與 final validator 各自維護不同的市場語意，令「基本面＋法人」在第一步被隱藏、只在紙上單後才被要求，因而耗盡模型預算。現已由共用的 Host 市場證據合約同時產生最小可用 capability、Host dispatch 與 final coverage check，避免任何特定提示詞例外；原生 `.command` UI 已以一般「技術、基本面、法人、風險＋1 股紙上買進」任務驗收為 8 項可用工具、第一步派出月營收／法人、4 步 `completed`、`live_execution_count=0`。

2026-08-29 修正 P70/P105 Agent Dock latency 投影：Observability window 的 `tool_latency_ms` 與 `model_latency_ms` 是 window 內的累計總值，先前被誤顯示為單次延遲而產生數百萬 ms 的錯覺。Dock 現在改讀 API 已提供的 `average_tool_latency_ms`／`average_model_latency_ms`，原生桌面 UI 已直接驗收為 7,693 ms／5,787 ms；只修正 operator display，沒有改動 durable metric、SLO sample 或任一交易資料。

2026-08-29 補強 A-011（維持 partial）：Session cold restore 的 fresh SQLite 目標現在除了重建可瀏覽的 Session／title history／message／Run 索引列，也會保存同一份 secret-minimized、content-addressed 的完整 archive envelope。此 envelope 包含每一個 Run snapshot 的 events、evidence、artifacts、versions、forest 與其他 snapshot-only lineage；讀回會重新驗證 archive SHA-256、schema、cross-record IDs 與 session identity，SQLite trigger 禁止 archive envelope 修改或刪除。既有目標的任何相同 Session／Run／Message／archive ID 都會在第一筆 insert 前拒絕，故不會覆寫 live authority。尚未取得獨立乾淨機器與 off-host retention 的正式收據，A-011 不標示 complete。

2026-09-07 補強 A-011／R-002／GOV-005（維持 partial）：Agent checkpoint 壓縮資格收斂為 `completed` Run；具有 retry 路徑的 `failed`，以及 `cancelled`、`interrupted`、waiting、active 與 bounded partial Run 均完整保留 checkpoint 及 event payload。維護 transaction 在 commit 前驗證 Run／event 資料列數、受保護 checkpoint 數與實際刪除差額，並在 commit 後執行完整 `quick_check`。正式 runtime 因 pending event 保持 blocked，未執行刪除；異機還原、離站保存與長期保留證據仍未取得，因此三項不改為 complete。

2026-08-29 推進 R-010（維持 partial）：四種 deterministic chaos recovery 收據（process kill、network partition、disk full、timeout）現在可寫入與 Agent runtime 相同的 SQLite append-only ledger；Host 會重新驗證每份 SHA-256、四個 scenario 的完整性，以及 durable state、order safety、operator receipt 三個不變條件。Agent `/api/agents/chaos-recovery` 與 Dock Observability 只讀投影最近演練：完整恢復顯示 `recovered`，缺 scenario／不變條件或 blocked receipt 則顯示 `blocked` 並產生 `chaos_triggered` operational alert。桌面 UI/API 沒有執行 process kill、斷網、磁碟填滿或 timeout injection 的入口；這些破壞性 fault 仍只能在受控 staging 演練，故尚無 staging recovery receipt、R-010 不標示 complete。

2026-08-29 推進 R-006（維持 partial）：OperationalAlertRouter 現在接到正式 Agent runtime SQLite、實際 filesystem 壓力、Host-owned broker runtime metrics 與 model-drift monitor；`/api/agents/operational-alerts` 與 Agent Dock Observability 同步顯示目前告警、不可變歷史、未觀測 broker scope、live-order gate 與 on-call delivery 缺口。沒有 broker metrics 的 paper-only 桌面環境會明確顯示 `not_configured`，不會把「沒有接入真實 broker」偽報成 feed/reconciliation/unknown-order 故障；已觀測到的 stale feed、reconciliation mismatch、unknown order 仍會產生 hash-verified receipt 並阻擋 live submission。on-call sink、production broker metrics 與 staging chaos-recovery receipts 尚未取得，故不標示 complete。

2026-08-29 修正 P1 本機紙上目標收尾：使用者明確要求「安全／最小」紙上模擬、只指定單一標的但未指定買賣與數量時，Host 僅能建立固定的 1 股零股買進沙盒單，且 durable receipt 會標記此為 Host-selected sandbox default；一般紙上交易仍必須明示方向與數量。已驗證的 preview 與 submit 也必須逐欄完全相同，模型若把 1 股改成 1 張、改變方向或 lot type，Host 會捨棄該呼叫並提交原 preview，不會讓模型改寫使用者的受限紙上操作。桌面 `開啟股市AI系統.command` 原生 UI 已以 2887.TW 驗收完成 1 股 odd-lot、filled 的本機紙上流程，終態為「已完成」，未等待外部驗收、未建立自動化、未讀取帳戶、未送往實盤券商。這不改變真實券商不可用的邊界。

2026-08-29 推進 S-004～S-010（維持 partial）：Open Stock AI 的正常 batch session 現在會從每一筆 decision 的 `market_snapshot.raw.portfolio_risk_materialization` 收集一份完全相同的 PIT 投組風險輸入，驗證涵蓋整個 watchlist 後產生單一 immutable receipt，並先保存到 SQLite，再由 portfolio construction 從同一 durable receipt 讀取 sizing、covariance、factor、concentration、ADV、CVaR 與 stress constraints。任何一筆缺少、不同、schema 不符或符號覆蓋不完整的輸入，都會保存明確的 `withheld` receipt、使所有 target 維持紙上研究拒絕狀態，不會再用 score 或即時 quote 自行估算。上游正式 PIT data/account-policy materialization 尚未供給此正常 session，因此不標示 complete。

2026-08-29 修正 P10/P12 完成後互動收尾：完成 Run 的 post-answer proposal 選擇「先略過」後，Host 會維持該 Interaction 的 durable `resolved` 狀態且不建立 child Run；Dock 同步將舊的可操作卡片替換為「已略過、目前結果保持完成」的終態訊息，避免原生 WebKit 重繪延遲把已完成工作誤顯示成等待外部驗收。新增 runtime skip、Browser 卡片替換與完整本機回歸測試；本機 `開啟股市AI系統.command` 原生窗口已確認 2887.TW 的 4/4 完成紙上流程不再顯示待決策卡。這只修正本機紙上／advisory 互動語意，不變更真實券商邊界。

2026-08-28 推進 R-005（維持 partial）：`DurableAgentRuntime` 每次 provider 執行結束都會把實測 latency 與 completed／failed／cancelled／suspended／partially_completed 終態寫入同一個 runtime SQLite 的 immutable `DurableSLOStore`；關鍵 `/api`／`/agent` request 亦由 Host middleware 記錄 latency 與 5xx outcome，而 paper-training 每次取得已驗證報價與 Paper Broker／OMS lifecycle 都會各自產生 data freshness／order lifecycle sample。資料平台的「評估資料新鮮度」命令也會以各來源的實際 `latest_success_at`（取最舊成功時間）回填 `data.freshness`；沒有成功時間、逾時、來源告警或 SLO 帳本寫入失敗都不會被偽裝成成功。`/api/agents/observability` 與 Agent Dock 顯示 hash-verified SLO evidence，且不自我觀測；重複讀取 Dock 不會新增新的 no-data report。broker feed 仍明確維持 `no_data`，`all_services_passing=false`，不會拿 Agent、API 或 paper 成功率冒充完整 SLO；正式 target owner acceptance 亦未具備，故不標示 complete。

2026-08-26 推進 A-005（維持 partial）：外部網頁／新聞／工具觀測在進入 provider-facing ContextBroker package 前，現在統一包成 `open_stock_ai.untrusted_content.v1` data-only envelope，保存來源、取得時間與內容 SHA-256；`instruction_authority=none`、`tool_authorization=host_validator_only`，並由 prompt policy 明確禁止外部文字產生或修改工具權限。Host 仍可把它當 evidence 分析，但模型不能把其中的 `ignore previous instructions`、假工具 JSON 或其他 hostile text 當成 runtime instruction。已加入 hostile corpus／tamper／provider transcript tests；完整 production gate 仍等待 reviewed web/news corpus 與真實 provider runtime receipt，故 A-005 不標示 complete。

2026-08-26 推進 A-006（維持 partial）：memory candidate 現在保存 evidence trust、provenance 與 Host verification 狀態；明示 `untrusted_data`、external/model source 或低於門檻的候選會以 `quarantine` 決策寫入 durable audit，但狀態為 `quarantined`、不會進入 active retrieval 或取代既有 memory。只有 Host 明確驗證才可釋放，同時保留原始來源與 quarantine reason。已加入 in-memory、SQLite restart 與高分惡意 memory tests；完整 gate 仍等待 reviewed malicious corpus 與真 provider-to-memory receipts，故 A-006 不標示 complete。

2026-08-26 再補強 A-007：`host_validated_mutation` postcondition 不再因 `order_id`、`schedule_id`、`confirmed` 或其他鬆散欄位直接通過，只接受 Host validator 產生、`accepted=true` 且完整欄位／SHA-256 驗證成功的 receipt；缺少或多出欄位也會 fail closed。`broker.order.propose` 與 `broker.order.cancel_proposal` 現在也回傳綁定精確 arguments、result 及 proposal store before/after state 的可驗證 receipt；不存在的取消只產生 `accepted=false`，不會被當成成功 mutation。非 broker 舊 mutation surface 與 live provider receipt 仍待遷移，因此 A-007 維持 partial。

2026-08-29 推進 A-007（維持 partial）：所有經統一 Host capability registry 執行的 mutation，現在在既有 request/result/before/after receipt 驗證後，再取得不可竄改的 provider-scoped domain receipt。此 receipt 綁定 provider、tool、category、execution backend 與原始 Host receipt SHA-256；因此 scheduler、notifications、UI、browser、skills、Git、project/terminal/web、paper、broker 與本機 external-runtime 等既有 mutation surface，都會在 durable `validation.*` event 中留下同一份 Host authority 的領域證據，並拒絕任何欄位或雜湊遭改寫的收據。Provider 不能自行宣稱成功；仍必須先通過 Host 的 state transition／execution checks。真實外部 side effect 的 provider-origin receipt 與帳戶持有人驗收尚未取得，故不標示 complete。

2026-08-29 原生 UI 驗收修正：Agent Dock 的 approval record 以 `<run_id>:<approval_id>` 做隔離鍵，但批准按鈕曾以 raw approval ID 查詢，導致 Host 已完成核准步驟後，桌面 UI 沒有重新 hydrate／重新訂閱同一 Run，而殘留「等待批准／有失敗分支」的舊投影。現在批准後會以 scoped record 找出 run、先讀取 durable snapshot、再從最新 sequence 接回 stream；不會將安全批准後的成功 validation 誤顯示為未修復失敗。

2026-08-29 原生 UI 迴圈修正：對 `ui_task`，所有已規劃的可執行 UI node 都取得 Host 驗證 bridge receipt、沒有未恢復失敗後，Host 會直接產生受限的完成摘要與 completion evidence，不再等待模型重複描述同一個已完成的 `ui.*` 呼叫。這只適用本機 UI bridge；不擴張為 Automation、paper order、外部通知或真實券商操作的自動完成權限。

2026-08-26 推進 A-008（維持 partial）：新增 Host-owned `CostBudgetManager` 與 `RunawayExecutionGuard`。模型 turn 會在 provider dispatch 前後扣除估算 input／output cost，工具 dispatch 會扣除 tool cost；global、session、provider、tool 任一上限超過即 atomic fail closed。recursive depth／node、總 tool calls、重複 tool signature 與 no-progress turns 也會阻擋後續執行，並把 cost／runaway snapshot 留在 Run state 與結果中。現有測試覆蓋新 guard 與 chaos 邊界，但 reviewed provider cost schedule 及真 provider runaway chaos receipt 尚未取得，故 A-008 不標示 complete。

2026-08-20 推進 E-016（維持 partial）：Paper OMS 現在將融券放空與一般賣出持倉明確分開。新空單沒有 account-scoped borrow locate（標的、可借量、驗證時間、到期、recall、年費率）就 fail closed；一經成交會將可借量保留到不可變的 short-borrow lot，依最新 mark 每日計提借券費並避免同日重複扣款。recall 到期後會標示既有空單為 `recall_due`、拒絕任何新增空單，但仍允許買回回補；回補才會 FIFO 釋放保留的借券。台股 Paper UI 也新增「融券放空／買回回補」入口，未附 locate 時會清楚拒絕。這是本機可稽核的 paper contract：尚未接上帳戶持有人核實的券商／TWSE locate、費率、recall 與歷史可借量來源，因此不宣稱可做真實 short execution 或 PIT short replay。

2026-08-20 補齊 E-015：具交易所結算契約的台股 Paper OMS 現在將經濟現金、已結算現金、待付與待收分離保存，依官方交易日曆在正常交易的 T+2 11:00 才把款項由 pending 轉為 settled。買進款會先保留；賣出待收款在交割前不會成為下一筆買進可用資金。每筆成交與 pending／settled transition 都有 durable SQLite receipt，UI 也會顯示可用現金、已結算、待收待付與 T+2 狀態。這是本地 paper DVP accounting，不聲稱已模擬券商信用額度、融資或真實交割帳戶。

2026-08-20 補齊 E-013：台股 Paper Broker 已在具完整交易所行情契約的委託路徑強制 tick、整股 1,000 股倍數、零股 1–999 股、漲跌停界線、盤中／盤後時段、集合競價 match receipt 與 ROD 收盤失效。零股不可再以一般整股 last trade 假裝成交；它只接受限價 ROD 並等待零股集合競價回報。停牌／處置／漲跌停流動性仍以不可變 official restriction ledger 驗證。此項仍是本地 paper simulation，沒有把 queue priority 或真實交易所成交保證誤稱為已實作。

2026-08-20 補齊 E-012：本地 Paper Broker 現在將 `submitted → acknowledged → open/triggered/partially_filled → filled/canceled/replaced/rejected/expired` 寫入 durable SQLite lifecycle。替換不會覆寫原單：原單保留既有 fill 並標記 `replaced`，新單只取得剩餘數量且以雙向 order ID 關聯；顯式到期、拒絕及每個事件都有可重播 audit record。紙上交易 UI 同步顯示已成交／剩餘量、到期時間與限價單改價入口。這是本機 paper execution state machine，不代表真實券商帳戶、交易所 queue 或實盤成交能力。

2026-08-13 補齊 E-010：live broker pre-trade gate 現在把完整 Host-observed context、計算後的價格／名目金額／ADV participation／預估曝險、每個 pass/fail check 與 reasons 一起封裝成 content-addressed receipt。OMS 在任何 broker I/O 前驗證 receipt 與精確 intent 的 SHA-256 綁定，然後以 SQLite 不可更新／不可刪除表保存；逐項 adversarial matrix 覆蓋報價時效、phase、數量、名目金額、價格 collar、ADV、symbol／account exposure、daily loss、strategy／model authorization 與 sellable inventory。這讓 E-010 的程式與負向測試條件完成，但不改變真實券商帳戶仍關閉的安全邊界。

2026-08-13 補齊 E-011：除了逐筆 Host-signed human approval，live OMS 現在還需要短時效的 Host-signed restricted-live activation receipt。receipt 綁定單一 broker／account、TWSE 現股 allowlist、多頭、單筆／每日名目上限與 signature；OMS 在 broker I/O 前檢驗並以 SQLite immutable tables 記錄實際使用。Agent capability registry 仍永久拒絕實際 broker submission，且 `config/capability_status.yaml` 仍停在 PAPER；此項完成的是不可由 LLM 偽造的 approval boundary，不是啟用真實交易。

2026-08-26 補齊 A-011：在既有 78 項 P1/P2 release checklist 中加入 Session archive 的直接證據。Session archive 現在能匯出完整 decision lineage、驗證 content/lineage hash，並提供不覆蓋 live store 的 cold restore projection；2026-08-29 已可 materialize 到全新 authoritative SQLite runtime database，並在同一 authority 保留 immutable full snapshot envelope。獨立乾淨機器 restore receipt 與 off-host archive retention 仍列為 blocker。README 的 generated capability block、`/api/system/capability-status` 與 `full_release` gate 都以此 YAML 的 124 項總清單計算；任何一項未驗收都會阻擋 release。

2026-08-26 推進 R-002（維持 partial）：SQLite authoritative storage 現在使用 SQLite online backup API 建立一致快照，並以 backup bytes SHA-256、schema fingerprint、`PRAGMA integrity_check` 與 receipt hash 形成可驗證證據；restore drill 會在乾淨目標重建資料、重新驗證 schema/integrity，且拒絕覆寫既有目標。這已由本機 tamper、existing-target 與 clean-target restore tests 證明，但正式備份目的地、排程、保留政策與異地災難復原尚未配置，因此 R-002 維持 partial。

2026-08-26 推進 R-003（維持 partial）：既有 SQLite DB 在 startup 跨 schema version 時，會先產生 R-002 verified backup，再以連續的 `_migration_1`～`_migration_46` source SHA-256 manifest 建立 migration safety receipt；migration commit 後 receipt 才標為 `completed`，若過程失敗則自動以驗證過的 backup 原子回復、保留失敗資料庫與 WAL/SHM 作 forensic evidence，receipt 維持 `prepared`。另新增 migration backup cleanup planner／executor：依 database 分組保留最新 N 個已完成且 hash／integrity 驗證成功的 pair，執行前再次驗證，永不清理 `prepared`、損壞、缺失或未知檔案，並輸出 hash-bound dry-run／execution report。已由 legacy DB、manifest continuity、竄改 receipt、失敗回滾與 cleanup retention tests 證明；production retention 執行、異地保存與長期 cleanup receipt 尚未取得，因此 R-003 維持 partial。

2026-08-26 補強 E-014（維持 partial）：Paper execution model 現在可接受獨立的 `historical_order_book_replay.v1` receipt，並驗證 snapshot／PIT availability／source revision／provider verification、depth levels、available quantity、matched quantity 與 queue-ahead 的 hash 綁定；若 caller 的情境數值與歷史收據不一致則 fail closed。execution receipt 另外把 historical replay verification 與既有 immutable empirical impact calibration 分開標記，沒有兩者時仍可做明確的 paper scenario，但 `execution_evidence_eligible=false` 並列出 blocker，不把 scenario input 偽裝成交易所 queue replay 或 empirical calibration。歷史 order-book corpus、provider attestation 與正式校準樣本仍未接入，因此 E-014 維持 partial。

2026-08-26 補強 E-016（維持 partial）：short locate 現在可明確標記 `execution_mode=historical`；此模式必須帶有四條 borrow availability／rate／recall／fee 的 hash-verified coverage receipt，且需 provider verified、涵蓋同一 symbol 與 PIT 日期範圍，否則 preview／reserve fail closed。一般 paper locate 不被誤標成歷史回放；production account-owner verified locate、rate、recall、fee feed 與完整歷史可借量仍未接入，因此 E-016 維持 partial。

2026-08-26 補強 E-018（維持 partial）：order reconciliation service 現在可使用 SQLite immutable receipt／event-claim store，startup、periodic 與 event-driven run 都沿用同一 OMS snapshot boundary；receipt 重新啟動後可驗證並查回，同一 event ID 在新 process 仍只會處理一次。production broker scheduler 與每個支援帳戶的 provider snapshot receipt 尚未接線，因此 E-018 維持 partial。

2026-08-26 補強 R-004（維持 partial）：TraceRecorder export 現在包含每個 span 的 W3C `traceparent`、bounded span count 與 `export_sha256`；`verify_trace_export` 會驗證 export hash、trace ID、span ID 唯一性、parent 只能指向先前 span 且 traceparent 與 span context 一致。production OpenTelemetry collector 與跨服務 trace backend 尚未配置，因此 R-004 維持 partial。

2026-08-26 補強 R-005（維持 partial）：新增 `DurableSLOStore`，以 SQLite immutable ledger 保存 data/API/Agent/broker/order observation 與 hash-verified SLO report，重啟後可重算並產生 dashboard projection；缺 sample 仍明確是 `no_data` 且列入 missing/breached services，不會被當作通過。live runtime ingestion、dashboard delivery 與 production owner target acceptance 尚未完成，因此 R-005 維持 partial。

2026-08-26 推進 R-007（維持 partial）：新增共用 provider／source／broker scope circuit breaker，具備 closed → open → half-open 狀態、單一 recovery probe、指數 backoff、scope 隔離與 hash-verified decision receipt；本次已把 `UnifiedBrokerGateway` 的 registry、health、capability、行情、帳戶、預覽、送單、修改、取消、reconcile 與 logout 入口全部接到 guard，provider／資料來源 call-site 全接線及 production chaos receipt 尚待完成，因此 R-007 維持 partial。

2026-08-26 補強 R-007（維持 partial）：OpenAI-compatible／external Agent provider 現在以 provider ID 與不含憑證的端點 origin hash 形成 circuit scope；不同端點的 failure 不會互相開路或互相封鎖，而同一端點的 generation 與 health 共享同一個 circuit，使 health 只能作為 half-open recovery probe，不能繞過 outage 保護。已加入跨端點 failure-isolation regression；production failure storm 與 recovery receipt 仍未取得。

2026-08-26 推進 R-005（維持 partial）：新增統一 SLO target／observation／report contract，覆蓋 data freshness、API、Agent、broker feed 與 order lifecycle；報告重算 p95 latency、success ratio、最大 freshness，缺少 observations 會回報 `no_data` 而不是通過，並以 SHA-256 綁定。現有 live ingestion、dashboard delivery 與 production owner target acceptance 尚待接線，因此 R-005 維持 partial。

2026-08-26 推進 R-006（維持 partial）：Operational alert router 現在可將 feed stale、reconciliation mismatch、unknown order、database pressure、model drift 與 chaos signal 寫入 SQLite append-only ledger；跨 process 重啟仍以 alert fingerprint 去重，alert 與每次 on-call sink attempt 都有 SHA-256 receipt，dispatch failure／缺 sink 會明確留下失敗證據，不會誤報送達。production alert sink、on-call routing 與 chaos recovery receipt 仍未配置，因此 R-006 維持 partial。

2026-08-26 推進 R-009（維持 partial）：load-test baseline 現在以 content-addressed JSON artifact 保存 operation、p95／錯誤率／吞吐、sample count、runtime environment 與 capture time；檔案使用 exclusive create、重啟可驗 hash 並禁止既有 artifact 被覆寫，regression report 仍對缺資料 fail closed。staging runner、CI execution 與 retained production baseline 尚未配置，因此 R-009 維持 partial。
2026-08-26 推進 R-009（維持 partial）：新增 `load_test_runner.py`，可用固定 request rate／concurrency／duration 對 HTTP endpoint 執行本機或 staging 測試，彙整 nearest-rank p95、error rate、throughput，輸出 immutable hash-verified report；仍缺 staging 執行與 retained baseline 證據，故維持 partial。
2026-08-26 推進 R-007（維持 partial）：Fugle WebSocket 現在連同實際 handshake 一起通過 shared external transport guard，斷線／握手例外仍以 fail-closed 方式釋放連線；仍缺正式 provider runtime／failover receipt，故維持 partial。
2026-08-26 推進 GOV-006（維持 partial）：OMS 新增可強制啟用的 Host-approved change-set binding；order intent 會在 durable persistence 前綁定 code／model／data／risk-policy 四個版本 hash，並可由同一 SQLite governance store restart 讀回；其餘 order writers 尚未全部接線，故維持 partial。

2026-08-26 推進 R-008（維持 partial）：新增共用 endpoint／tool／provider scope rate-limit governor，支援已驗證 window、未知政策 conservative interval、maximum concurrency、429 exponential backoff 與 hash-verified decision receipt；並修正 receipt 的 `in_flight` 不再固定為 0，broker gateway 已使用同一 governor，非 broker 外部 call-site 接線與各 provider 的正式政策收據尚待完成，因此 R-008 維持 partial。

2026-08-26 推進 R-007/R-008（維持 partial）：同步與 async 官方資料 loader 新增共用 `ExternalTransportGuard`，讓 TAIFEX institutional、TWSE/TPEx、MOPS、TDCC、LINE／Telegram、TWSE MIS／Fugle quote 與 Fugle intraday candles 共用 process guard；client 會依 response status 更新 circuit、釋放 rate slot，並在 429/5xx 進入退避，未通過 admission 時 fail closed。其餘 websocket／其他非 HTTP transport、各 provider 正式 rate policy 與 production failure-storm receipt 仍待完成，故不提前宣稱 R-007/R-008 complete。

2026-08-26 推進 R-007/R-008（維持 partial）：Agent `web.fetch` 依目的 host、`web.search` 依 DuckDuckGo／Bing provider scope 經共用 async `ExternalTransportGuard` admission，並補上 provider-scope 接線測試；既有 websocket／其他非 HTTP transport、正式 provider rate policy 與 production failure-storm receipt 仍待完成，故不提前宣稱 R-007/R-008 complete。

2026-08-26 推進 R-007/R-008（維持 partial）：短券／當沖歷史 loader 的 TWSE／TPEx HTTP endpoint 改以日期與資料類型使用獨立 `ExternalTransportGuard.call_sync` scope，並補上三個 TWSE endpoint 的接線測試；既有 websocket／其他非 HTTP transport、正式 provider rate policy 與 production failure-storm receipt 仍待完成，故不提前宣稱 R-007/R-008 complete。

2026-08-26 推進 R-007/R-008（維持 partial）：n8n authenticated management API 的 health、settings、callback 與 workflow lifecycle request 也統一經過 host/path scoped ExternalTransportGuard.call_sync，並補上 secure request 接線測試；正式 provider rate policy 與 production failure-storm receipt 仍待完成，故不提前宣稱 R-007/R-008 complete。

2026-08-26 推進 R-007/R-008（維持 partial）：Google News event feed 以固定 provider scope source:google_news:events 經共用 ExternalTransportGuard.call_sync，並補上事件解析接線測試；正式 provider rate policy 與 production failure-storm receipt 仍待完成，故不提前宣稱 R-007/R-008 complete。

2026-08-26 推進 GOV-003（維持 partial）：新增 capability promotion ladder，嚴格限制 RESEARCH → PAPER → SHADOW → BROKER_SANDBOX → RESTRICTED_LIVE → PRODUCTION_LIVE 逐級前進；promotion 需要非 Agent 的人類核准與該級全部 evidence，Host 可因 reconciliation／data stale 等原因 downgrade，兩種轉換都有 content-addressed receipt。durable receipt store 與 staging acceptance 尚未全 runtime 接線，因此 GOV-003 維持 partial。

2026-08-26 完成 T-009：新增 machine-readable release gate，輸出 ledger hash、阻擋 requirement IDs 與 deterministic exit status；目前 full release 仍會 fail-closed。

2026-08-26 推進 GOV-004（維持 partial）：新增 immutable approved artifact history，啟用只接受人工核准 artifact，rollback 立即選回上一個 approved artifact 並產生 tamper-evident receipt；`SQLiteGovernanceStore` 現在把核准、啟用歷史與 rollback receipt 持久化到同一個 migrated authoritative SQLite，重啟後會驗證並恢復狀態，更新／刪除由 trigger 拒絕。所有 strategy/model deployment writer 尚待接入此治理 writer，故仍維持 partial。

2026-08-26 推進 GOV-005（維持 partial）：新增 bounded signal projection 與 content-addressed critical retention；一般 signal 僅保留最近 500 筆，execution receipt 不被 pruning 丟棄且可依 hash 重建；durable scheduler、off-host archive 與所有 critical writers 尚待接線。
2026-08-26 補強 GOV-005：`SQLiteRetentionStore` 現在將 retained content 與 retention receipt 保存到同一個 migrated authoritative SQLite；重啟後會驗證 content／receipt hash，普通 signal 可依明確 retention 操作刪除，但 critical execution content 與 retention receipts 由 immutable trigger 保護。durable scheduler、off-host archive 與所有 critical writers 尚待接線，故仍維持 partial。

2026-08-26 再補強 GOV-005：`BrokerOrderManagementGateway` 現在可明確注入 `ContentAddressedRetentionLedger`，每次 OMS durable state projection 都會以 `stock_ai.broker_oms_execution_state.v1` 保存完整 intent、broker mapping、fills、reports、approval 與 receipt 的 immutable critical snapshot；`require_critical_retention` 會拒絕沒有 durable OMS／retention store，或兩者不在同一個 durable database 的啟動。重啟後可從同一 SQLite 驗證並重建 snapshot。durable scheduler、off-host archive 與所有其他 execution-critical writers 尚待接線，故仍維持 partial。

2026-08-26 推進 GOV-003（維持 partial）：`/api/system/capability-status` 現在會讀取同一個 runtime SQLite 的 `SQLitePromotionReceiptStore`，回報 durable promotion receipt 數量、最新 receipt、持久化等級與 YAML configured level 的一致性；若歷史與設定不一致，會列出 activation blocker，不會因資料庫內的 receipt 自動越級。所有 runtime mode 的 staging acceptance 與正式 promotion receipt 仍待外部驗收，故仍維持 partial。
2026-08-26 推進 GOV-003（維持 partial）：首次啟動且 runtime SQLite 尚未存在時，`/api/system/capability-status` 會先初始化檔案型 `SQLitePromotionReceiptStore`；重啟後仍可讀取同一份 promotion ledger。`:memory:` 路徑仍明確維持 non-durable／fail-closed，所有 runtime mode 的 staging acceptance 與正式 promotion receipt 仍待外部驗收。

2026-08-26 補強 R-007／R-008（維持 partial）：OpenAI-compatible model discovery 在未由 caller 注入 guard 時，現在使用 process-shared `default_external_transport_guard()`，不再為每次 discovery 建立私有 circuit／rate governor；自訂 guard 仍可供 deterministic tests 或隔離 provider scope 使用。其餘 production provider policy receipt 與所有外部 endpoint 的完整共用接線仍待驗收。

2026-08-26 補強 R-007／R-008（維持 partial）：Agent provider registry 現在讓 OpenAI-compatible 與 external-agent provider 共用 process-shared `default_external_transport_guard()`，並以 registry wiring test 驗證兩者引用同一 guard；正式 provider policy receipt 與全部外部 endpoint 的 production failure-storm 驗收仍待完成。

2026-08-26 補強 R-007／R-008（維持 partial）：預設 `UnifiedBrokerGateway` 現在透過 process-shared `default_broker_transport_guard()` 共用 Broker circuit／rate-limit state；仍可注入隔離 guard 供測試或獨立 process 使用，並以多 gateway wiring test 驗證預設路徑不會分裂准入狀態。正式 broker policy receipt 與 production failure-storm 驗收仍待完成。

2026-08-26 補強 R-007／R-008（維持 partial）：直接建立 `OpenAICompatibleAgentDriver` 或 `ExternalAgentDriver` 時，現在同樣注入 process-shared `default_external_transport_guard()`；因此不經 provider registry 的 production driver path 也不能繞過 provider circuit breaker 與 rate-limit governor。provider policy receipt、其餘外部 call-site 與 production failure-storm recovery receipt 尚待驗收，故維持 partial。

2026-08-26 補強 R-007／R-008（維持 partial）：模型清單探測的 OpenAI-compatible／Ollama probe 現在也以端點 origin 的 SHA-256 scope 隔離；不同遠端主機不會共用 circuit／rate state，scope 不會外洩私有 hostname 或 URL path。相同端點內不同協定 probe 仍維持各自的保守節流，避免為了探測 fallback 而跳過 unknown-policy guard。正式 provider rate policy receipt 與 production failure-storm recovery receipt 仍待驗收。

2026-08-26 補強 R-007／R-008（維持 partial）：新增 production network-client audit；所有 `src/stock_ai` 中使用 HTTP／WebSocket／`urllib.request` client 的 provider、source、broker、automation 與模型 driver 模組，都必須宣告 shared external 或 broker transport guard，否則測試失敗。壓測 runner 刻意不納入，因為它必須如實量測呼叫端設定的速率。至此移除「尚未全接線」內部 blocker；每個正式 provider rate policy 與 production failure-storm recovery receipt 仍待驗收。

2026-08-26 補強 GOV-006（維持 partial）：正式 runtime 的 `TradeStore`／`PaperOMS` 現在注入與 runtime governance 相同的 durable change registry；啟用 strict mode 時，沒有已核准 change ID 的 paper order 會在寫入帳本前 fail closed。Paper Broker 亦會在 durable ticket reload 後保留並轉交 `change_id` 給 Paper OMS，避免 acknowledge／restart 後遺失版本 binding。直接 OMS、broker handoff、重啟 retry 與 runtime same-authority 測試皆覆蓋；預設 paper runtime 尚未由帳戶本人 provision 一個已核准的 active change set，因此不能宣稱 GOV-006 complete。

2026-08-26 補強 GOV-003（維持 partial）：啟動階段現在讀取同一 SQLite 的 immutable promotion receipt ledger；最後一張驗證通過的 receipt 會決定有效 capability level 與 execution mode，沒有 receipt 才回退 YAML 的 paper baseline。重啟後可由 paper → shadow receipt 正確進入 shadow，環境變數仍不可跳級，broker／live execution 仍維持 fail-closed。每一 capability promotion 的 staging acceptance receipt 尚未取得，因此 GOV-003 不提前標示 complete。

2026-08-26 補強 GOV-004（維持 partial）：紙上訓練介面與 API 現在可讀取 runtime-owned SQLite 的 approved artifact ledger，並由人工輸入核准者、原因後在指定的策略或模型 lane 回滾。策略／模型各自只會找回同一 lane 的前一個已核准 artifact，避免交叉回滾；操作會立即追加 immutable receipt，重啟後仍可重建。production rollback drill 與其餘 deployment surface 實機驗收尚未取得，故不提前標示 complete。

2026-08-26 補齊 A-010：`BrokerOMSStore` 現在提供 durable intent、idempotency、broker-order 與 report lookup；配置 durable store 的 `BrokerOrderManagementGateway` 每次讀取／對帳／重試前都從該 store refresh，process-local mapping 僅為快取，不再是第二個可寫真相。跨 instance idempotency、report 去重與 broker mapping 測試已加入；live order matrix 因而由 transitional 提升為 canonical。正式 live broker、reconciliation 與 promotion 仍由各自的 E/R/GOV/T 門檻控管。

2026-08-26 推進 GOV-006（維持 partial）：新增 immutable change set，固定 code／model／data／risk policy 四個 SHA-256，並將每筆 order 綁定 exact change hash；`SQLiteGovernanceStore` 現在把 change set 與 order binding 持久化到同一個 migrated authoritative SQLite，重啟後會驗證完整 hash，更新／刪除由 trigger 拒絕。所有 OMS order writers 尚待接線，故仍維持 partial。

2026-08-26 補強 GOV-006（維持 partial）：change-set order binding 現在具有 crash／timeout retry 等冪性；既有 order 只要仍指向同一個已驗證 change hash 與 code／model／data／risk-policy hash，就回傳第一次的 immutable receipt，不會因新的 `bound_at` 誤判衝突。強制 binding 的 `BrokerOrderManagementGateway` 同時要求 durable OMS 與治理帳本共用同一個 SQLite authority，避免版本 receipt 遺失或分裂。Paper 與其他 order writer 尚未全部接線，故仍維持 partial。

2026-09-01 完成 T-006：保留既有 model metric-tolerance replay，並新增獨立隔離 Qlib `risk_analysis(mode=product)` receipt。真實 FinRL rollout 與真實 Qlib portfolio workflow 皆取回 exact return path、input SHA-256、年化頻率與上游／runtime module fingerprint；Host 僅比較定義相同的 annualized return、max drawdown，任何 path、欄位或容忍度不相符皆 fail closed。實際 FinRL、Qlib E2E 均已通過。

2026-08-26 推進 T-007（維持 partial）：正式登錄五券商 offline worker／capability／order contract matrix 與 fail-closed sandbox config；五家官方 sandbox/provider 的實際回條仍待帳戶本人與官方 SDK probe。

2026-09-07 推進 T-008（維持 partial）：GitHub hosted run `34139640836` 已在 exact commit `6bf13664` 對 DB、API、回測各保留 100 筆 baseline 與 100 筆 candidate，逐面檢查 p95、p99、error rate、throughput 與 regression threshold，產生可獨立驗證的 content-addressed receipt，artifact 保留 90 天。依目前禁用模型的限制，model runtime surface 明確標為 deferred，未用替身冒充完成。

2026-08-13 補齊 SEC-002：`Core research and Agent safety` workflow 已使用 `fetch-depth: 0` 及 `gitleaks/gitleaks-action@v2` 掃描完整 Git history；本機以相同 `gitleaks git . --log-opts='--all' --redact=100` 實跑 506 commits／43.8 MB，結果沒有 leak。`.gitleaksignore` 只保留 reviewed historical false-positive fingerprints，測試會拒絕泛用 ignore；這與 SEC-001 的歷史憑證輪換／rewrite 證據是兩項不同門檻。

2026-08-26 補強 SEC-002（維持 complete）：完整歷史掃描發現一個舊 Broker OMS 測試的 idempotency fixture 被 `generic-api-key` 規則誤判；經逐一檢視後只加入該 commit／檔案／規則／行號的單一 fingerprint，沒有擴大 path 或 regex allowlist。本機重跑 680 commits／45.51 MB 為 zero findings；GitHub Actions 因帳務／額度無法啟動屬外部 CI evidence blocker，且不改變 SEC-001 仍待 owner 完成 rotation／history rewrite 的狀態。

2026-08-13 補齊 Q-003 的帳戶費率證據基礎：`BrokerCostScheduleReceiptStore` 以 immutable SQLite receipt 保存 broker/account、venue/product/lot/side 範圍、有效日期、費率、外部文件 SHA-256 與本機 persistence hash。`TaiwanSecondaryMarketCostSchedule` 不再把 row 的費率數字和任意 schedule ID 當作 verified；只有有效、帳戶範圍相符且已持久化的 receipt 才能讓 broker/exchange fee 成為 execution evidence。尚無真實已審核券商／交易所收據，也沒有 empirically calibrated impact artifact，因此 Q-003 繼續維持 partial。

2026-08-25 追加 Q-003 的反誤導保護：缺少 venue/product/lot/account cost context 的 OHLCV-only `BacktestResearch` baseline 不再套用固定 20 bps，也不再回報 `transaction_costs_included=true`；它明確輸出 `transaction_cost_schedule_missing`，只有 `ExactStrategyReplay` 的逐列成本收據可以進入 execution evidence。Q-003 仍維持 partial，直到真實帳戶費率與經驗 impact calibration 完成審核。
2026-08-26 補強 Q-003 官方來源 capture：`config/taiwan_market_cost_rules.yaml` 的 Ministry of Finance／TWSE 來源現在各自保存抓取時間與原始內容 SHA-256，`TaiwanMarketCostRulebook.receipt()` 會輸出 `official_source_receipts`，缺少 capture hash 或時間即拒絕載入。這只補足公開規則文件的不可變來源鏈；真實帳戶費率 receipt 與 empirical impact calibration 仍未取得，因此 Q-003 維持 partial。

2026-08-13 補齊 D-001：所有 MOPS archive financial endpoints（monthly revenue、income statement、balance sheet、cash flow）都有 archive publication timestamp unavailable 的 source contract，統一按 acquisition-only 保存並禁止 historical PIT；live MOPS 的 financial datasets 則一律 require original `published_at`。此規則以 registry-to-endpoint test 固化，避免把日後下載的 archive backfill 冒充為歷史可知資料。

2026-08-13 補齊 D-002 的歷史 coverage 基礎：news/event lake 拒絕缺少原始 `published_at` 的資料，並要求 per-source、日期範圍、原始時間完整性與 provider document hash 的 immutable coverage receipt。未帶 receipt 的新聞可供 live 顯示但會在 PIT projection 被標記 `news_historical_coverage_receipt_missing`；尚無已審核完整 provider historical coverage receipt，故 D-002 維持 partial。

2026-08-20 補齊 D-007：每個 PIT `FeatureRecord` 現在保存公式、公式 SHA-256、完整 immutable input revision IDs 與當次 projection source 的 code SHA-256。資料集將這些特徵節點與 revision edges 正規化成可重建的 provenance graph，並把 graph SHA-256 納入 manifest identity；研究 runtime 在任一 feature lineages 遺失、公式不符、輸入 revision 遺漏或 code SHA 被替換時 fail closed。

2026-08-26 完成 D-008：TWSE market calendar 現在納入 2023–2026 官方 `holidaySchedule` 年度快照、2024-07-25 凱米與 2024-10-02 山陀兒兩次全日休市公告，以及官方交易制度／休市頁的 regular-session 09:00–13:30 content-hash receipt；捕獲的官方期間沒有特殊／半日 session，系統保留明確的空例外清單而不臆造交易事件。即時行情和 schedule guard 共用同一份不可變快照，避免週末以外的官方休市被誤判為交易中。`TaiwanMarketCalendar.receipt()` 會同時暴露 holiday 與 session receipts，D-008 已由 status ledger 標為 complete。

2026-08-26 推進 D-003：chip PIT coverage 現在可透過 `ChipPITCoverageReceiptStore` 將 institutional、margin、borrowed-short 與 TDCC stream 的 coverage／missingness manifest 以 receipt hash 寫入 immutable SQLite；重複寫入只接受相同 payload，修改或刪除會被 trigger 拒絕。查詢流程會保存本地 receipt，但 TDCC 歷史覆蓋與各來源 production endpoint review 仍未完成，故 D-003 維持 partial，exact replay 仍 fail closed。

2026-08-26 推進 Q-003／E-014／D-010：新增 `MarketImpactCalibrationStore`，以 immutable SQLite 保存帶 source receipt 的成交觀測，依 PIT participation bucket 計算 adverse fill impact 的 median／P90，並以 minimum sample、provider verification、observation manifest 與 artifact hash 決定是否可作 execution evidence。未具 provider attestation、樣本不足或缺 participation input 時一律輸出 partial／blocker；這補足本地校準 artifact 邊界，但沒有把 scenario input 或未審核 paper data 宣稱為券商／交易所 empirical calibration，三項要求仍維持 partial。

2026-08-26 推進 D-002：news event lake 現在會把每個 provider 的 ingestion event-set audit（provider count、unique event manifest、缺失原因與 PIT eligibility）保存到 immutable SQLite；重複 audit 只接受相同 hash payload，修改／刪除會被 trigger 拒絕，production gateway 也會使用此 store。這只讓本地 coverage decision 可重啟追溯，沒有新增或臆造歷史 provider coverage，因此 D-002 仍維持 partial。
2026-08-26 再補強 D-002 的證據閘門：immutable store 現在會在驗 hash 前檢查 audit 的 source、事件數、唯一 manifest、排序後 blockers、complete 狀態與 coverage receipt 是否互相一致；即使呼叫者重算了合法 SHA-256，也不能保存「無 provider receipt 卻 complete」或數量矛盾的 audit。這仍不等同於取得 provider 全歷史 coverage receipt，故 D-002 維持 partial。

2026-09-01 補強 D-003 的 TDCC 真實性界線：TDCC 官方股權分散表公開頁明示歷史查詢檔案保存期間只有一年；公開 OpenAPI 也不提供歷史發布時間證明。因此 `tdcc`／`tdcc_holding_distribution` 現在把 365 天來源保留期寫入不可變 availability contract，但一律不認證為 production PIT。即使呼叫端自行填入 `published_at`，chip coverage 仍會 fail closed，避免一小段公開查詢或人工時間戳被誤報為完整籌碼歷史。已在公開來源外另行保存、且可證明當時發布時間的不可變資料可由未來經審核的專用 provider contract 另行接入；目前 D-003 仍維持 partial。

2026-09-01 補強 Q-003 的帳戶費率證據邊界：僅有 immutable 的本機 `BrokerCostScheduleReceipt` 現在明確只能支援敏感度分析；即使 scope、雜湊與持久化皆有效，沒有同時綁定已審核的券商帳戶費率文件及交易所費率文件，就不能把成本報價標記為 execution evidence。只有 `ReviewedBrokerCostScheduleReceipt` 的完整審核鏈可達到結構上的成本證據資格；這不取代帳戶本人、券商與實證市場衝擊的外部驗收，故 Q-003 維持 partial。

2026-09-01 再補強 Q-003 的市場衝擊校準來源：`provider_verified` 不再是能由呼叫端單獨宣告的充分條件；普通 hash-bound source receipt 僅可供研究。要讓 observation 進入 execution-eligible empirical calibration，receipt 必須同時綁定券商 reconciliation、交易所 execution document 與帳戶持有人 review receipt SHA-256。缺一項、即使其餘欄位與本機雜湊有效，calibration 仍 fail closed。這是本地結構／完整性 gate，不表示已取得真實帳戶文件或已完成外部 empirical calibration，故 Q-003 仍維持 partial。

2026-09-01 補強 D-002 的新聞覆蓋證據邊界：coverage receipt 的單一 provider document hash 不再足以讓 ingestion 宣稱 PIT 完整；未經覆核的本機 provider coverage claim 一律只留作研究／顯示。PIT replay 需要同時有 provider contract receipt 與帳戶持有人 review receipt SHA-256 的結構鏈，否則 event count 完全相符也會保留 `provider_coverage_review_missing` blocker。這只防止本機自我宣告冒充完整歷史覆蓋；尚未取得 provider-wide 真實覆蓋資料與審核收據，故 D-002 仍維持 partial。

2026-08-25 推進 N-003（維持 partial）：n8n callback 現在由 workflow Code node 產生 timestamp、execution-scoped nonce 與 exact-body HMAC，Stock AI middleware 會驗證 callback-only derived secret、五分鐘時鐘偏差、原始 body 簽章與十分鐘 nonce replay TTL，重複 nonce、過期或 body 竄改均 fail closed。此次已完成本機 security／workflow contract tests；尚未在本機 n8n 實際執行產生一筆帶簽章 headers 的 callback receipt，因此不把靜態 workflow graph 宣稱為完整 provider 驗收。

2026-08-25 推進 N-004（維持 partial）：n8n public API key 與 Stock AI callback secret 現在由不同設定值管理；local owner bootstrap 會建立獨立 callback secret，缺少它時 deployment readiness fail closed，`scripts/rotate-n8n-callback-secret.sh` 只替換 callback secret 並明確要求重新部署既有 workflow。尚未在實際 n8n 執行 rotation、redeploy 與 signed callback 的完整連續流程並保存 receipt，因此不宣稱 N-004 完成。

2026-08-25 推進 N-005（維持 partial）：n8n workflow graph 與 headless runtime 現在關閉成功／錯誤／逐節點／手動 execution payload 留存，Host callback payload 先經既有 recursive secret redaction，並將 n8n execution history 限制為 168 小時、2,000 筆與 1 MiB 顯示上限，同時啟用 pruning。尚未在實際 n8n 執行資料庫完成 prune 與 secret scanner receipt，因此不宣稱 N-005 完成。

2026-08-25 推進 N-006（維持 partial）：一般 Agent scheduler 與產品 Automation scheduler 現在共用 `market_calendar=taiwan` 的休市日判斷，支援 `run_once`／`catch_up`／`skip` misfire policy；週末、TWSE 官方假日與重啟後的逾期 occurrence 會持久化延後到下一交易日，不會在閉市日建立 Run。n8n callback envelope 也攜帶 calendar name，Host 端再次 fail closed。已完成 deterministic holiday／restart regression；尚未在實際 n8n 排程執行、重啟與 callback 連續流程保存 provider receipt，因此不宣稱 N-006 完成。

2026-08-26 原生 UI evidence（P14／P18／P61）：使用桌面 `開啟股市AI系統.command` 啟動目前 `main` 的 `6485b5d`，Computer Use 以遠端 `gpt-oss:20b` 執行純 advisory 研究任務，Run `AR-bd34f53ef239471d99bcb163d23c9522` 完成 7/7 steps 與 4 個 Branch completion criteria。Host/UI 最終投影為 `recommendation_bucket=data_blocked`、`execution_permission=blocked`、`execution_boundary=analysis_only_no_order_submission`；沒有建立 Automation 或交易權限。這是本次實機 evidence，不取代 P14／P18／P61 其餘互動與多支線矩陣驗收。

2026-08-29 原生 UI evidence（P14／P16）：使用桌面 `開啟股市AI系統.command` 啟動本機分支，以遠端 `gpt-oss:20b` 在同一個 Agent Session 依序輸入「分析台積電的技術與風險。」與「改為比較台積電與台新新光金的風險差異。」。Composer 不再讓使用者選擇「建立新 Run／加入目前 Run」；Host 自動將第二句送入 active Session、取消過期 root Run，並建立新的 Forest／替代 Run。原生 Task Forest 最終顯示修正後目標及 1/1 完成；取消原因投影為 Host 因使用者修正替換舊任務，非誤稱使用者手動取消。這是 root hard-steer 實機 evidence；P15／P17 的無關 branch 並行觀察與 P94 soft-steer child Run 仍待各別驗收。

2026-08-20 核實並登錄 D-009：Security lifecycle 已以 official TWSE／TPEx master、ETF、warrant 與 delisting payload 建立 entity、identifier 有效日期與 lifecycle event registry。轉板可藉 business identity 合併；同碼重用會保留多個歷史 entity 並按 `as_of` resolve；跨 venue 歧義、ETF share class 與無效統編均 fail closed。相關 SQLite lifecycle／entity resolution／historical universe tests 已覆蓋上述邊界。

2026-08-20 推進 D-010（維持 partial）：market impact receipt 現在強制標示 simulation mode；目前資料只能是 `spread_adv_proxy`，會明示 `depth_data_status=unavailable` 與 `depth_simulation_eligible=false`。這避免把即時五檔或日線 volume 偽裝成可回放 order-book depth；尚未有歷史 L1/L5/depth snapshot corpus 或 empirical depth-impact calibration，故不能完成 D-010。

2026-08-26 推進 D-010（維持 partial）：`cost_model.py` 新增 canonical L1/L5 snapshot hash、買賣盤深度消耗、PIT depth VWAP 與 impact-bps receipt；只有帶有一致 SHA-256、snapshot ID、capture time 且深度足夠的 retained snapshot 才能進入 `order_book_depth` simulation，否則仍 fail closed。這補足本地 depth contract 與 deterministic tests，但尚未匯入歷史 L1/L5/depth snapshot corpus，也未完成 empirical depth-impact calibration，因此 D-010 仍維持 partial。

2026-08-20 推進 D-011（維持 partial）：借券賣出／還券／餘額歷史現可進入一般 chip PIT，但新增獨立 short-execution receipt，強制要求 borrow availability、rate、recall 與 fee history。現有官方 activity 資料不會再被解讀為「當時一定可借並可開新空單」；四條 execution streams 未匯入前一律拒絕 short execution replay。

2026-08-26 推進 D-011／E-016（維持 partial）：新增 `ShortBorrowHistoryReceiptStore` 與四條 borrow availability／rate／recall／fee history coverage receipt；每條流都保存 observation manifest、required value、PIT eligibility 與 provider verification，SQLite trigger 禁止修改／刪除。四條流即使都有本地輸入，未通過 provider verification 仍不能進入 execution replay；目前沒有帳戶持有人核實的真實 borrow feed，因此兩項仍維持 partial。

2026-08-26 推進 E-017（維持 partial）：新增 `MultiBrokerRouter`，以 broker health、quote freshness、available quantity、fee、latency、allowlist／route permission 建立 deterministic route receipt；買單以成本最小、賣單以淨收入最大，tie-break 固定為 latency 與 broker ID。receipt 明確保留 `execution_authority=none` 且不送單；真實券商 health／流動性／費用／permission provider receipt 尚未取得，故 E-017 維持 partial。

2026-08-20 推進 D-012（維持 partial）：資料平台現在從 immutable failover attempts、checkpoint、cache freshness 與 revision history 產生每個來源／資料集的 SLO、lag、預期分區缺漏與 revision-correction anomaly dashboard；資料平台頁面會直接顯示自動重新評估出的告警，未觀測到成功資料不會被當成正常。此告警目前只在本機 data-ops UI／API 中可見，尚未配置有 receipt 的 operator notification delivery，故不能宣稱外送告警已完成。

2026-08-20 推進 D-005（維持 partial）：每個新建的 market-intelligence 決策現在都會帶有 hash-verified、SQLite immutable 的資料品質收據，分別保存 freshness、完整性、已宣告異常、來源衝突觀測狀態與 confidence。首頁／個股決策 UI 會呈現收據 ID 與 certification 狀態；尚未取得跨來源品質報告的 legacy quote loader 一律標記 `not_observed`／`partial`，不會寫成來源一致或完整 production quality evidence。

2026-08-25 推進 D-005：共用的 `stock_ai.data_quality_contracts` 現在由 market-intelligence、screener 與 legacy market summary/detail overview 同時使用；每個通過條件選股結果與 legacy overview/detail 都保存相同 schema 的 hash-verified quality receipt，decision detail UI 也顯示 freshness、completeness、anomaly、source disagreement 與 confidence。legacy summary 尚未取得同時點獨立跨來源觀測，其他尚未接入的 decision surfaces 仍維持 partial，未被標成完整品質證據。

2026-08-26 推進 D-005：新增共用 `decision_quality_receipts` SQLite ledger，摘要與 screener 的新決策會保存相同 hash-verified receipt；同一 receipt 可安全重放，但 SQLite trigger 禁止修改與刪除，且 API 重啟後仍可查回 surface、symbol 與原始 payload。legacy summary 現在會嘗試取得獨立 Yahoo 同日觀測，只把它當品質比較證據、不取代官方／MIS 主行情；同日觀測會保存 primary/secondary close、差異、容許值與來源，衝突則 certification blocked，無 secondary 或日期不符仍明確是 `not_observed`。因此 D-005 仍維持 partial，不把 provider 缺失誤標為通過。

2026-09-07 完成 D-005：Market Intelligence 對宣告 v1 receipt schema 的新 snapshot 強制核對 decision 數量、每筆 receipt 是否存在，以及 snapshot／symbol 綁定；缺漏或錯綁會回滾整筆 snapshot。兩個 immutable ledger 在讀取時都重新驗證 payload hash 與列 identity，同一 receipt 也不能靜默改綁另一個 surface。跨來源資料不存在仍只產生 `not_observed`／`partial` receipt，這是已驗證的降級結果，不再誤列為系統實作 blocker；原生首頁決策詳情會顯示 receipt ID、certification 與來源衝突狀態。證據見 `docs/validation/native-d005-decision-quality-receipts-20260907.md`。

2026-08-20 補齊 T-002：移除了 collection-time 動態 `xfail` 標記及八個過時斷言；它們的安全責任已由現行研究安全、外部來源鎖定與 Codex radar 風控測試承接。聚焦回歸為 `79 passed`，完整本機回歸為 `1302 passed, 4 skipped`，均無 `xfailed`／`xpassed`；另有靜態測試防止 `tests/conftest.py` 再以 collection hook 隱藏舊失敗。四個 skip 仍依本文件規則保留為各自環境／外部驗收的未完成訊號，不能據此宣稱整體 release 完成。

2026-08-20 核實 Q-010～Q-016：研究 replay 已使用連續帳本指標、bootstrap／Deflated Sharpe／Holm-Bonferroni 顯著性 gate、PIT regime receipts、historical universe 及官方價格復權 receipts。Q-015 現在只接受具 timestamp、available_at、source、dataset、revision 的獨立 benchmark 與 cash-rate 對齊資料，缺漏時不再以分析標的本身冒充 benchmark；alpha、beta、IR 因而可重現且會 fail closed。Q-016 新增可重現的 circular block bootstrap 情境報告，呈現 VaR、CVaR、最差終值、回撤分位數與跨批次穩定性。UI 同步顯示 Alpha／Beta／IR／CVaR；直接模組、replay、promotion gate 與 UI 靜態測試都覆蓋這兩項驗收。

2026-08-25 核實 M-003：在專案 `.runtime/venv-macos-arm64` 安裝 vendored TradingAgents 依賴後，以明確 opt-in 的隔離 graph runtime adapter 實跑 `external/TradingAgents`；遠端 `http://192.0.2.1:11434/v1` 的 `/v1/models` 回應 200 並列出 `gpt-oss:20b`，單一 `market` analyst graph 對 `2330.TW` 實際完成並產生 `Buy` 決策、state keys 與結果 hash。receipt 保留 `execution_authority: none`、`execution_boundary: research_evidence_only_no_order_authority`；未設定相容性 API key、依賴、連線或 schema 失敗時仍只回報 disabled／failed，不得假稱模型輸出。實際 smoke receipt 見 `docs/integration/tradingagents-release-smoke-20260825.md`。FinGPT 與 FinRobot 的第三方原始碼仍只作 read-only contract 參考，未驗證 runtime 不會被呈現為 inference。

2026-08-20 核實 S-004：投組權重不再只把單檔核准權重正規化。每個可提出的 paper target 必須有 volatility target、最大損失 risk budget 和 fractional-Kelly cap 的 deterministic receipt；任何 PIT input 缺漏都會將 target 歸零並標為 `withheld`，不能由 rule score 或 policy maximum 補出部位。這仍是 partial，因為 live 使用的 versioned PIT risk inputs 尚未被 materialize。

2026-08-20 核實 S-005：portfolio construction 現在還需取得 versioned PIT covariance matrix、factor exposure／limit matrix、`as_of` 與 dataset manifest 才能提出 target。optimizer 先以單檔 cap 取上界，再以 portfolio volatility scale 與 linear factor caps 收斂；矩陣非對稱、欄位缺漏或違反任何約束時全部 target `withheld`。目前沒有 production materialization 的 covariance、factor 與 account context，所以 S-004／S-005 均維持 partial，不能被 UI 說成投組已可執行。

2026-08-26 推進 S-004～S-010（維持 partial）：新增 `PointInTimePortfolioRiskContextBuilder`，要求每檔的 returns、factor exposure、ADV 與 sizing inputs 都帶 `available_at`／source revision，且與同一 dataset manifest、account-scoped paper policy 及核准收據綁定；只在所有資料於 `as_of` 已知、時間戳對齊且 content hash 通過時，才物化 covariance、factor、concentration、liquidity、CVaR 與 stress context。`PortfolioConstruction` 會把同一份 context 的 per-symbol sizing inputs 傳給 RiskSizer，三個 risk engine 也會驗證 materialized context hash；任何未來資料、篡改或缺欄位都歸零／withheld。這補上本地 production path，仍等待全 active universe 的正式 PIT risk materialization 與真實帳戶政策證據，因此不標成 complete。

2026-08-26 推進 S-004～S-010（維持 partial）：新增不可變 `PortfolioRiskContextStore`。verified 與 withheld 的 PIT portfolio-risk context 都會以 receipt SHA-256 為 immutable key 寫入 SQLite；重啟後可依 receipt deterministic replay，載入時會重新驗證 schema、狀態與內容 hash，資料庫列遭修改或刪除時由 SQLite trigger 阻擋，繞過 trigger 的竄改則在讀取時 fail closed。這補上 risk context evidence 的 durable restart path，但不把尚未完成的全 active universe 生產物化、歷史流動性／因子資料與帳戶政策審核誤標成 complete。

2026-08-20 核實 M-007／M-011：FinRL／Qlib 的實際研究 runtime 現在會建立不可變 model version，將 artifact、PIT dataset、feature schema／lineage／code hash、worker 回報 metrics 和 research-only approval contract 一起綁定；產生該 runtime 的 signal 也保存 model-version IDs 與 experiment ID，不會再有無版本模型輸出。實驗收據同時記錄 FinRL／Qlib seed、package lock、非識別性的 CPU／平台 runtime fingerprint，並保存 metric-tolerance replay contract。框架／加速器 deterministic-kernel 的獨立認證尚未存在，因此 bitwise reproducible 仍明確為 false，不能被誤稱已保證位元級重播。

2026-08-20 核實 M-010／M-012：每個實際模型 runtime 的 receipt 現在同時是可搜尋、可比較的統一 Experiment entity，連結 immutable model versions、metrics、重現契約與目前 promotion 狀態；Champion/challenger 不會在訓練或 inference 時自動變更，必須另寫入含 human decision ID、approver、時間與理由的不可變 receipt，且 deployment 只能明示為 shadow 或 production，execution authority 一律保持 `none`。研究 UI 同步顯示推廣狀態與部署邊界。

2026-08-20 核實 M-008／M-009：規則分數只有在 PIT outcome observation 已知於 cutoff 的 empirical isotonic receipt 下才會成為 calibrated probability；receipt 會顯示 Brier 與 ECE，缺少或無效時 UI 與風控都保留「未校準」語意。實際 model runtime 則必須附 feature、prediction、performance drift 監控；任一 PSI、Brier degradation 或 hit-rate decline 超出閾值時，研究 evidence 自動標為 disabled，缺監控收據也會 fail closed。這個停用只影響模型 evidence，沒有給模型任何交易執行權限。

2026-08-13 補齊 D-003 的籌碼 PIT 契約鏈：法人、融資融券、借券／融券與 TDCC 四條 stream 現在各自依「實際 record 的 source_id」產生 availability／missingness receipt，必須同時具備歷史可用性、production contract 與本機 `acquired_at` 才可取得 exact replay 資格。借券／融券已正式列入 flow warehouse 與 TWSE／TPEx 官方日資料契約；缺任一 stream 不會被標成 complete 或以零補值。實際 TDCC 歷史 coverage／收取證據與所有來源端點的 production review 尚未完成，故 D-003 維持 partial。

2026-08-13 補齊 D-004 的端點層 availability audit：`market_data_sources.yaml` 的每一個 active endpoint 必須有 `source_id + domain` 的明確 availability contract；SourceRegistry 在啟動時會拒絕任何缺漏或多餘的 endpoint contract。48 個目前 active endpoint 的 receipt 都有 immutable contract SHA、basis 與 PIT eligibility，不再由 nullable availability 或 source default 代替。這確保新端點不會靜默繞過審核。

2026-08-25 追加 Q-008/D-004 的 runtime audit receipt：`DataAvailabilityRegistry.audit_receipt()` 將 active endpoint coverage、research dataset coverage、registry SHA 與 blockers 合併成 `stock_ai.data_availability_audit.v1`，SourceRegistry 與 UnifiedResearchDataGateway 都會暴露同一份收據。它讓研究重播能證明使用的契約集合，但不會把尚未完成外部 production review 的資料標成 complete。

2026-08-25 追加 Q-008/D-004 的 exact dataset contract：48 個 active dataset ID 現在各自保存 `endpoint_dataset` scope 的 canonical contract SHA，`SourceRegistry` 啟動時會拒絕任何缺漏、重複或多餘的 dataset contract；`verify_audit_receipt()` 仍會在外層 receipt hash 通過後再次驗證內層契約，防止只重算外層 hash 就掩蓋契約內容被改寫。Q-008 與 D-004 的 availability-contract acceptance 已完成；provider-wide 歷史覆蓋仍由 D-002/D-003 個別追蹤。

2026-08-25 追加 D-002 的匯入完整性閘門：新聞 coverage receipt 現在會把 provider 宣告的事件數與本次 immutable lake 寫入的唯一 `event_key` manifest 綁定成 `stock_ai.news_history_ingestion_coverage_audit.v1`。數量不符、重複事件或 publication timestamp 不完整時，事件仍可供 live 顯示，但所有對應 PIT eligibility 會 fail closed；這補強了部分資料誤被當成完整歷史 coverage 的風險，尚未取代已審核 provider 全歷史 coverage receipt。

2026-08-25 追加 D-003 的籌碼 stream audit：法人、融資融券、借券／融券與 TDCC 每條 stream 現在都產生 `stock_ai.chip_stream_ingestion_audit.v1`，保存唯一觀測鍵 manifest、原始 record manifest、日期範圍、來源與 missingness blockers；整體 receipt 另有 SHA-256，重複觀測、缺日期、缺 acquired_at 或未認證 availability 都會撤銷該 stream 的 PIT eligibility。這強化了本地 coverage／missingness 證據，但不把 TDCC 歷史取得或來源 production review 誤標成完成。

2026-08-31 推進 E-006 台新優先路徑（維持 partial）：台新不再只是通用 `DeclaredBrokerAdapter`；新增只載入帳戶本人於專案外安裝之官方 `PY_TradeD` wheel／`PY_Trade_package` 的唯讀 quote SDK Worker。Worker 只接受 Host 的 `keychain://<service>/<account>` 參照，於隔離程序記憶體解析登入資料後呼叫官方 `MarketDataMart`、`Sol_D.Login(..., "TWS")` 與 `RCode.OK`，不回傳帳號、密碼、OTP 或憑證內容。SDK 缺失、Keychain 參照缺失或登入拒絕均 fail closed；即使官方 Login 接受，也只標為待帳戶本人行情驗證與 immutable receipt，不能宣稱行情、帳務或交易能力。實際 SDK binary、帳戶本人線上認證、唯讀行情／Session receipt 與其餘四家 official worker 仍未取得，E-006 維持 partial。

2026-09-01 補齊 E-006 台新唯讀行情 probe 的官方 SDK 閉環（仍維持 partial）：先前 Worker 僅做到 `Login`，現在 Host 授權收據與 Keychain reference 同時存在時，可對單一格式嚴格限制的 `TWSE:代號` 依官方順序呼叫 `Login`、`Subscribe`、等待帶有相同代號的 `OnMatch`／`OnUpdateLastSnapshot` callback、`Unsubscribe`、`DisConnect`。逾時、訂閱拒絕或任一清理失敗均 fail closed；結果只輸出事件種類、官方公開欄位名稱及 callback payload SHA-256，絕不輸出價格、帳號、密碼、OTP 或憑證內容。這只是可實際執行的隔離驗證程序，沒有真實 SDK、帳戶本人驗證與 immutable integration receipt 前，能力仍為 `null`、E-006 仍為 partial，交易能力也仍關閉。

2026-08-13 補齊 E-006 的外部 SDK 證據閘門：新增 immutable SQLite `BrokerIntegrationReceiptStore`，一筆可用的 SDK/唯讀 probe receipt 必須繫結帳戶持有人確認、校驗通過的官方 SDK artifact、隔離 worker release SHA 與 read-only probe SHA；帳號僅能以 masked alias 保存，絕不保存憑證、token、原始帳號或 SDK binary。receipt 可以直接由既有 `BrokerSdkArtifactReceipt` 建立，杜絕手填雜湊冒充官方安裝。沒有每家券商實際 sandbox 或 production-readonly 的收據時，五家 adapter 繼續維持非 operational，E-006 僅為 partial。

2026-08-13 補齊 E-007 的 session lifecycle 證據鏈：新增 immutable `BrokerSessionLifecycleStore`，只接受由 E-006 durable SDK integration receipt 連結的外部 session evidence。狀態機強制 `certificate_verified → login_readonly → refresh/reconnect → reconnect_snapshot_recovered → logout` 的合法序列，transport reconnect 未完成 snapshot recovery 前不會回到 connected；資料庫重啟後仍保留完整排序與 immutable audit。尚無帳戶持有人實機完成五家券商的證書、登入、refresh、logout 與 reconnect 收據，故 E-007 僅為 partial。

2026-08-13 補齊 E-008 的 reconciliation evidence：`BrokerAccountReconciliationReceiptStore` 將 broker account snapshot、Host ledger、reconciliation result 與 E-006 integration receipt 的 SHA 固化成 immutable SQLite record。它僅留 identity hashes 和 masked account，不保存任何原始現金、持倉、訂單或成交資料；未連結官方 SDK evidence 的 reconciliation 一律拒絕保存。內存比對已具備 restart-safe audit 基礎，但尚未由真實券商週期性驗證 cash／position／order／fill，故 E-008 僅為 partial。

2026-08-13 補齊 E-009 的行情復原 evidence：`BrokerMarketFeedRecoveryReceiptStore` 將 sequence observation、gap range、snapshot recovery 與 source failover 的雜湊固定成 immutable SQLite evidence，並要求每筆收據先連結 E-006 的官方 SDK integration receipt。snapshot recovery 必須指向同一 stream 的 durable gap，且每個 gap 只能被復原一次；這些收據不保存 raw quote payload。尚未以真實券商 feed 完成 chaos、缺口、快照復原與 failover 實機收據，故 E-009 僅為 partial。

2026-08-20 補齊 D-006 的 research dataset identity：PIT manifest 升級為 v2，明確保存每一 feature domain 的 schema version、排序後 feature partition SHA-256、順序敏感 replay-row SHA-256 與整體 dataset SHA-256。ResearchModelRuntime、ResearchEngine 與 target/stop calibration 在使用資料前都會重算 materialization；任何 feature、row、schema、count 或 manifest identity 改動都 fail closed。D-006 現在具備可驗證的 exact restore 契約，但這不會解除其他 P0/P1 資料或券商證據門檻。

2026-08-13 的 Agent 認知閉環修正新增 Proposal Evaluation 專用 strict Provider schema、可合法承載 follow-up objective／Automation semantic intent 的 `interaction_proposals.arguments`、Host-grounded `ReflectionCheckpoint`，以及回答後提案的 durable Interaction／同 Session child Run。這補上了原本「類別與 UI 存在、真 Provider schema 或回覆 endpoint 卻不通」的斷點；Task Forest canonical execution authority、完整真 provider 與原生 UI 驗收仍分別保留為未完成門檻。

2026-08-13 的 UI 同步修正以桌面 `開啟股市AI系統.command` 啟動原生 App，Computer Use 實際操作首頁、個股圖表與 Agent Task／DAG。首頁圖表已改為與個股圖表共用 `CHART_THEME`，淺色主題的 workspace tabs、繪圖工具、決策摘要與 Agent 圖形區也改用同一組 semantic surface tokens；這只補強 P55／P73 的跨頁呈現一致性，不代表 P0–P105 整體完成。

2026-08-10 本次分支已從 `/Users/your-user/Desktop/AI股市系統-Agent測試版/開啟股市AI系統.command` 由 Finder 重建並啟動原生 **股市AI系統 Agent測試版**，Computer Use 可見操作 Session History、Task Plan、Automation 與 Evidence Graph。遠端 OpenAI-compatible Ollama `gpt-oss:20b` 完成 n8n 喚醒的同 Session 重新分析 Run `AR-a20cb3c0a5444f0d8932455c60d50553` 與 `AR-cb08a3f7402244b0ac31cc1783d7d583`；n8n workflow `hKG9Du0Meb8k7nKC` 的 execution 42／46／49 均成功回呼，Stock AI durable execution `AEX-2eedaccb0e17405e8b1673c940855f2f` 保留 `n8n:42`、Run、Evidence 與 decision gate 關聯。完整 pytest 為 `1051 passed, 3 skipped, 8 xfailed, 1 xpassed`。

本次已通過指定桌面專案、原生 UI、遠端 Ollama、真實本機 n8n、SQLite restart／compaction 與 deterministic Chaos catalog。外部 Email／Slack 等通知送達、多家 Cloud provider 真實端點及真正殺死 Browser／provider process 的災難演練沒有可用授權或受控環境，仍維持「需驗收」，不得由本次 Ollama／n8n 證據外推。

## 2026-08-10 本次分支驗證範圍

Computer Use 以完整 App 路徑附著 `/Users/your-user/Desktop/AI股市系統-Agent測試版/.runtime/apps/Stock AI Liquid Glass.app`，未使用瀏覽器作為 UI 驗收。可見終態包含 `gpt-oss:20b · openai-compatible`、2881.TW Run 5/5 完成、官方月營收 Evidence、兩個 Evidence Node 與驗收後暫停的 n8n Automation。真實交易始終停用。

2026-08-20 核實 canonical Forest identity：`DurableAgentRuntime` 先寫入 SQLite 的 `forest_id`、root `branch_id` 與 `objective_id`，現在會在 provider dispatch 前注入 `RuntimeForestAuthority`；首次 Run 不會被誤當 resume，checkpoint 若帶有不同 in-memory Forest ID 也不能覆蓋該 durable identity。這只完成 ID 收斂；execution child Branch 的 transition source 尚未完全統一，P0/P5 仍維持部分落地。

2026-08-20 推進 E-014（維持 partial）：Paper OMS 現在會以穩定的 broker fill-report ID 去重，將每一次受 `available_quantity` 限制的成交累積到同一筆 durable order，並精確更新剩餘數量、加權均價、cash ledger 與 position。ROD 可跨 process restart 繼續成交；IOC 僅取消餘量；FOK 在可用數量不足時不會產生成交。這仍是可重播的本地模擬：`available_quantity` 尚非歷史 order book／交易所 queue，成交機率與 market impact 也尚未以真實成交資料校準，因此不得表示為真實券商成交品質。

2026-08-26 推進 E-014（維持 partial）：Paper Broker 新增 `paper_execution_model.v1` deterministic receipt。每個 market tick 可明確提供 `matched_quantity`、queue-ahead、`fill_probability`、seed、latency 上限與 market-impact／latency-impact bps；模型會以可重播 hash 判定成交機率、持久化 queue 剩餘量，並把 blockers、成交量與總 impact 寫入 broker event。這補齊本地 scenario／replay contract，但沒有把 scenario input 誤稱為歷史 order-book／exchange queue，也沒有宣稱 fill probability 或 impact 已由券商／交易所成交資料校準。
2026-08-26 同批推進 E-014（維持 partial）：Paper execution model 另接受明確的 `market_volume` + `participation_rate` 與可選 `volume_cap`；成交數量取 available、queue、參與率上限與 volume cap 的最小值，缺少成對輸入或超出範圍時 fail closed，並將所有限制寫進 durable broker event receipt。這是可重播的 scenario liquidity contract，不是歷史 order book／交易所 queue，也不代表已完成真實成交資料校準。

2026-08-26 推進 E-014／D-010（維持 partial）：新增 `HistoricalOrderBookReceiptStore`，以 immutable SQLite 保存已驗證的歷史 order-book snapshot，Paper Broker 可用 `historical_order_book_receipt_id` 從 store 重啟讀回並套用 queue／matched／available quantity；明示 receipt ID 但無法從 durable store 解析時直接阻擋成交。這補上本地 receipt persistence 與 lookup boundary，仍等待真實歷史 depth corpus、exchange queue replay 與 empirical fill/impact calibration，故不標成 complete。

2026-08-26 推進 D-012（維持 partial）：資料觀測新增 `data_observability_notifications` append-only ledger 與 `POST /api/data/observability/notify`。每個 SLO／lag／missing partition／revision alert 都會產生可重播的 attempt、provider receipt、去重與 SHA-256；沒有 operator sender 時明確記錄 `not_configured`，配置 sender 後才會記錄 `delivered`，不把本機 alert 評估冒充 Email／Slack 已送達。外部 operator channel 尚未配置，因此 D-012 仍維持 partial。

## 狀態定義

| 標記 | 意義 |
| --- | --- |
| 已有落點／需整體驗收 | 正式模組及對應自動化測試已存在，但仍須與完整系統一起重跑測試及 `.command` 驗收。 |
| 部分落地／需整合驗收 | Domain、持久化、投影或契約已有實作，但尚不能證明真實 Runtime、UI、外部服務端到端都完成。 |
| 需實機 UI | 已有前端或 Browser E2E 覆蓋，仍須從 macOS `.command` 啟動的本地專案親自操作。 |
| 需真實 provider | 現有測試僅覆蓋 adapter／contract／mock；必須實際呼叫指定 provider／模型。 |
| 安全邊界 | 真實交易刻意維持停用；驗收目標是確認無法繞過風控與批准，不是送出實盤委託。 |

## 主要執行鏈

```text
Agent Dock / Session Message
  -> DurableAgentRuntime
  -> AgentOrchestrator + Host PlanGraph
  -> FinalAgentRuntime
       -> Objective Version
       -> Durable Task Forest projection / checkpoints
       -> Interaction / proposal arbitration / steering
       -> Evidence / provenance / KPI
       -> Artifact versions / selection
       -> Automation / notification lifecycle
  -> SQLite migrations and event stream
  -> Agent Dock task forest / decision / evidence / artifact / automation views
```

主要整合檔：

- `src/stock_ai/durable_agent_runtime.py`：Session／Run 生命週期、恢復、訊息、事件、排程與 Final Runtime 整合。
- `src/open_stock_ai/agent_runtime/orchestrator.py`：模型回合、PlanGraph、工具執行、研究、Reflection、Repair 與 Host completion。
- `src/open_stock_ai/agent_runtime/final_runtime.py`：P0–P105 領域資料的持久協調與 API 投影。
- `src/open_stock_ai/agent_runtime/forest/`：遞迴 Forest、Scheduler、Executor、Projector 與六層 Checkpoint。
- `src/stock_ai/agent_api.py`、`src/stock_ai/ui/static/js/features/agent/`：HTTP／SSE 與 Agent Dock 控制面。
- `src/open_stock_ai/storage/migrations.py`：P71 SQLite 核心表。

## P0–P9：產品、Session 與 Recursive Task Forest

| 規格 | 實作模組／目前證據 | 自動化測試落點 | 驗收與目前判定 |
| --- | --- | --- | --- |
| P0 最終產品定義 | `durable_agent_runtime.py`、`orchestrator.py`、`final_runtime.py` 串接 Session、Forest、互動、Repair、Memory、Automation 與 Artifact。 | 下列全部 Agent 測試群。 | **部分落地／需整體驗收**：P0 是 P1–P105 的總完成條件，不能因模組存在就宣告完成。 |
| P1 不可違反原則 | `contracts.py` 的 Host event、`policy_engine.py`／`approval_manager.py`、`proposal_arbitrator.py`、`memory/policies.py`、`automation/policy_engine.py`；completion gate 另拒絕未有證據的數字與 `X%`／`EPS of Y`／`TBD` 等金融佔位符。 | `test_agent_runtime.py`、`test_agent_runtime_v2.py`、`test_agent_approval_flow.py`、`test_agent_memory_final.py`、`test_durable_agent_runtime.py`。 | **已有落點／需整體驗收**：需實機確認無假進度、無 CoT 洩漏、無偏好硬規則及無危險繞過；2026-08-08 已由原生遠端模型摘要樣本補強佔位符攔截。 |
| P2 整體系統架構 | `session/`、`forest/`、`interaction/`、`research/`、`repair/`、`automation/`、`artifacts/` 與 Capability Registry／工具層。 | `test_agent_runtime_final_interaction.py`、`test_agent_runtime_final_repair_research.py`、`test_agent_automation_final.py`。 | **部分落地／需整合驗收**：架構元件齊備不等於所有真實能力已端到端連通。 |
| P3 Session 高於 Run | `session_store.py`、`agent_run_store.py`、`DurableAgentRuntime.create`／Session API；一個 Session 可持有多個 Run／Message。子 Run 訊息不再覆蓋 foreground root Run；舊資料若曾把 child 寫入 `last_run_id`，讀取時會沿 `parent_run_id` 回復主 Run。 | `test_durable_agent_runtime.py`、`test_agent_final_system_scenarios.py`、`test_child_run_messages_preserve_root_session_focus_and_repair_legacy_focus`。 | **本地原生重啟已驗收**：指定 launcher 重啟後，原生 Dock 仍顯示暫停中的 parent `AR-2be4c92d328c4484a8a32dec4bb5aeb4`，沒有被 cancelled child `AR-c36dd96644d64d6582f45ad375c2b31b` 搶走；畫面按下恢復後同一 parent 回到 running。 |
| P4 Objective Versioning | `session/objective_manager.py`、`FinalAgentRuntime.create_forest`／`steer`、P71 `agent_objective_versions`。 | `test_agent_runtime_final_interaction.py::test_objective_versions_are_immutable_and_branch_binds_revision`。 | **已有落點／需整體驗收**：需驗證 hard steer／topic correction 僅建立新 revision 且 Branch 綁定正確版本。 |
| P5 PlanGraph 升級 Task Forest | `forest/models.py`、`forest/projector.py`、`forest/executor.py`、`final_runtime.py`。Projector 將真實 Plan／Step Host event 寫入 Forest；`agent.run_subtasks` 會先建立 idempotent durable child Branch、Join，再以 child Run 綁定執行。`DurableForestStore.transition_branch` 是 child Run／Branch control 的唯一持久 transition boundary，原子更新 Branch、Local Plan、Join 與 provenance，並拒絕 terminal reopen。Runtime capability executor 亦接收同一 durable Forest/root Branch/objective ID，避免同 Run 產生另一組根 ID。 | `test_agent_forest_executor.py`、`test_agent_forest_projection.py`、`test_agent_runtime_v2.py::test_runtime_subtasks_link_parallel_child_runs_to_the_parent_forest`、`test_durable_agent_runtime.py::test_durable_runtime_injects_its_persisted_forest_identity_before_provider_dispatch`。 | **本地真實 provider Forest 與 delegation 已驗收／child transition 已統一**：父 Run `AR-1f9b961c4c4046729235b80df5eb2fa9` 透過 `agent.run_subtasks` 建立並等待 Critic child Run `AR-43f0e1beb0d54c62a183857590552273`；重啟後 UI／SQLite 的 Branch、Join 與 placeholder Step 都維持 completed。全 Run capability executor 的 in-memory scheduling snapshot 仍只作 checkpoint transport，多 child 的原生並行觀察仍列於 P7 待驗。 |
| P6 Branch Local Plan | `LocalPlan`／`LocalStep`、`agent_branch_plans`／`agent_branch_steps`、Projector。 | `test_agent_runtime_final_interaction.py::test_local_plan_is_ordered_and_branch_budget_is_enforced`、`test_agent_forest_executor.py`。 | **已有落點／需真實 provider**：需觀察模型產生的分支各自有局部 Step，而非只顯示 UI 分類。 |
| P7 Branch 內有序、Branch 間並行 | `forest/scheduler.py`、`ForestExecutor`、`LocalPlan.ready_steps`；`agent.run_subtasks` 以 `asyncio.gather` 啟動已綁定的 child Runs；Orchestrator 也會把同一 turn 內 Host 判定為 read-only、無副作用、無 approval／rollback 的獨立 sibling tools 放進有事件可稽核的平行 batch，其餘能力維持序列。 | `test_agent_forest_executor.py::test_branches_run_truly_in_parallel_while_each_local_plan_remains_ordered`、`test_same_turn_independent_read_only_tools_execute_in_parallel`、`test_same_turn_tools_with_declared_side_effects_remain_serial`、dependency test、subtask branch/run linking test。 | **正式執行鏈與原生 UI 已驗收**：barrier 測試證實兩個 read-only tools 必須同時開始才能完成，副作用 tools 的最大 active count 仍為 1；Run `AR-2f379275baaf4909973c04936d00d700` 的 SQLite sequence 24/47 與 79/100 分別留下兩批 `parallel_read_only` start/completed，UI 同步顯示平行工具 lifecycle。 |
| P8 Branch 遞迴與防爆 | `TaskForest.add_branch`、`BranchExecutionContext.spawn_child`、`ForestLimits`、duplicate detection／budget。 | recursive spawn、depth／active／node overflow tests in `test_agent_forest_executor.py`、`test_agent_runtime_final_interaction.py`。 | **已有落點／需真實 provider**：需測模型動態建立第二層以上 child branch，並實際觸發限制。 |
| P9 Join Node | `JoinNode.evaluate`、`ForestExecutor._resolve_joins`、Projector 的 durable join projection；Plan revision 新增 required Branch 時會重新開啟已完成 Join，同一 required set 則保留結果。 | conflict join／verification branch／partial join／Join revision tests in `test_agent_forest_executor.py`、`test_agent_forest_projection.py`。 | **本地 parent／Critic Join 已驗收／Conflict Join 仍需驗收**：父 Run `AR-1f9b961c4c4046729235b80df5eb2fa9` 等待 child Critic 完成後才合併，最終 2 個 Join 都 completed；互相矛盾來源觸發 verification／Conflict Branch 尚需實機情境。 |

## P10–P22：Reflection、Interaction、Steering 與 Capability

| 規格 | 實作模組／目前證據 | 自動化測試落點 | 驗收與目前判定 |
| --- | --- | --- | --- |
| P10 ReflectionCheckpoint | `interaction/reflection_checkpoint.py`、`orchestrator.py` 的公開 `reflection.created`／interaction 協定。 | `test_agent_runtime.py::test_model_can_pause_at_public_reflection_and_request_user_decision`、interaction tests。 | **本地真實 provider + UI 已驗收**：Run `AR-c3ea0c5af13f4b099efc9bbd98f9ec06` 只公開三案比較、理由與 Agent 暫定偏好，未顯示私有 CoT；Decision Card 等待使用者決定後才續跑。 |
| P11 Agent 先提出方案再反問 | `interaction/decision_checkpoint.py`、`FinalAgentRuntime.create_interaction`、`agent-decision-card.js`；`orchestrator.py` 另以 Host materialization 將使用者明確二選一綁回原始選項，並對「只請使用者選」的模型回覆只在同一 Run 內重問一次具體暫定建議。 | decision schema test、`test_agent_dock_browser.py::test_decision_card_accepts_preferred_option_and_free_text`、`test_host_materializes_explicit_choice_card_when_provider_only_lists_options_in_prose`、`test_host_reprompts_once_then_materializes_a_provider_choice_card_with_exact_user_labels`。 | **本地原生三路徑已驗收（2026-09-01）**：遠端 `gpt-oss:20b` 的初輪泛用卡與純文字建議都由 Host 重建為使用者原句中的「保守型／平衡型投資計畫」決策卡；Agent 建議、替代選項及自由文字均在同一 Run checkpoint 後完成，詳見 `docs/validation/native-p90-interaction-20260901.md`。這不構成投資、下單或 Automation 證據。 |
| P12 分段推理與乾淨回合 | durable `waiting_user_input`／`waiting_decision`、Decision checkpoint、resume from checkpoint。 | `test_durable_agent_runtime.py::test_waiting_decision_response_resumes_same_run_from_durable_checkpoint`。 | **本地真實 provider 已驗收**：Run 先以 `waiting_decision` 結束第一個 provider turn，Interaction 回覆後發出 `interaction.responded`、`run.resumed` 並開啟第二個 provider turn；最後 Host validation 通過。 |
| P13 Proposal Arbitration | `proposal_arbitrator.py`、`FinalAgentRuntime.record_user_proposal`、Durable Session message pipeline。 | proposal arbitration unit／durable persistence tests。 | **原生安全拒絕已驗收（2026-09-01）**：新 Advisory Session 輸入台新真實帳戶買進要求後，Host 不呼叫 provider、不建立 Run／Plan／paper order／Automation；Dock 保持 0/0 並顯示明確拒絕訊息。accept／modify／ask 及 active Forest 的 UI 互動仍各自維持驗收項目。 |
| P14 執行中永遠可輸入 | `agent-composer.js`、`agent-dock-controller.js`、`POST /sessions/{id}/messages`；active Run 時 Host 自動路由 Session message，Composer 不要求使用者選內部 Run 模式。 | current-run composer DOM／API contract tests、`test_agent_dock_state.py::test_current_run_composer_posts_a_session_message_with_precise_selection`。 | **本地原生 correction UI 已驗收（2026-08-29）**：一般自然語句與自然修正可在同一個 active Session 輸入並完成替代 Run；answer、append、control 與完全無關新問題仍待分別實機驗收。 |
| P15 Soft Steering | `SteeringRouter`、`FinalAgentRuntime.steer`，局部新增 child branch／Objective revision。 | `test_agent_runtime_final_interaction.py::test_soft_hard_and_fork_steering_only_touch_required_branches`、scenario G、linked follow-Run control tests。 | **本地 mid-run child Run 主要路徑已驗收**：局部支線建立 same-Session follow Run，pause／resume／cancel 都控制實際 task；仍需原生 UI 同時觀察無關 Branch 持續執行。 |
| P16 Hard Steering | 同上；以 target Branch 所屬 Forest（不是 Session 最新 child Forest）界定影響範圍，先 cancel 受影響 linked Run，再建立不重用 cancelled Branch 的 replacement Run，舊證據保留；root correction 亦會取消過期 provider Run、建立獨立替代 Forest。 | steering test、`test_hard_steer_cancels_affected_linked_run_before_starting_replacement`、`test_natural_root_correction_replaces_the_active_run`。 | **本地原生 root correction 已驗收（2026-08-29）**：自然「改為比較…」取消舊 root Run、顯示 Host replacement reason，並完成修正後 Run；仍需在原生 Composer 注入錯誤股票代號與 child-branch scoped correction。 |
| P17 Fork | `SteeringIntent.FORK_BRANCH`、Forest branch insertion／Objective revision。 | steering test、`test_agent_final_system_scenarios.py`。 | **部分落地／需真實 provider**：需確認兩棵比較支線並行執行並在同 Session 顯示。 |
| P18 自然語言三類輸出 | `orchestrator.py` model summary；Host event envelope；`agent-conversation-view.js`、timeline／decision view。 | event visibility／Dock reducer tests。 | **需實機 UI**：確認 Work Summary、Runtime Status、Decision／Result 視覺與資料來源分離。 |
| P19 Host event 是進度真相 | `contracts.build_runtime_event`、Orchestrator tool lifecycle、event reducer。 | `test_agent_runtime.py::test_orchestrator_streams_auditable_activity_without_private_reasoning_or_secrets`、event contract tests。 | **需實機 UI**：故意讓模型文字聲稱工具動作，UI 不得在無 `tool.started` 時顯示真執行。 |
| P20 Runtime Event Taxonomy | `contracts.FINAL_RUNTIME_EVENT_TYPES`，包含 session／objective／forest／branch／join／plan／step／tool／research／reflection／interaction／proposal／repair／automation／memory／artifact／result。 | `test_agent_event_contract.py`、Forest projection／Dock reducer tests。 | **已有落點／需整體驗收**：需檢查所有實際路徑都發出 taxonomy 事件，不只常數存在。 |
| P21 Multi-intent Capability Composition | `provider_capabilities.py::CapabilityComposer`、Orchestrator 多 task kinds。 | `test_agent_runtime_final_repair_research.py::test_capability_composition_is_multi_intent_and_phase_scoped`。 | **部分落地／需真實 provider**：以「分析＋通知」確認同一回合具 market／research／automation 能力。 |
| P22 階段揭露 Capability | `CapabilityComposer.disclose` 的 research／decision／automation／confirmation gate。 | 同 P21。 | **部分落地／需真實 provider**：確認 confirmation 前看不到 create／send／paper order；批准後才揭露。 |

## P23–P34：Research、Artifact 與 Automation 設計

| 規格 | 實作模組／目前證據 | 自動化測試落點 | 驗收與目前判定 |
| --- | --- | --- | --- |
| P23 External Research 標準能力 | `research/planner.py`、Orchestrator research plan、market／web／browser capability。 | research planner test、`test_agent_runtime.py::test_current_information_cannot_finish_from_search_results_without_opening_sources`。 | **部分落地／需真實 provider／網路**：需最新股票問題同時使用內部資料與外部來源。 |
| P24 Evidence-first loop | `research/models.py`、`evidence_graph.py`、`observability_store.py` 的 durable evidence/provenance；Orchestrator 將模型 provider、Host tool provider、worker receipt 與實際 source／URL／publication time 分離。 | evidence graph／source missing／forest provenance restart／canonical multi-source provenance tests。 | **官方工具本地原生驗收／真實網頁 conflict 仍待驗**：Run `AR-2f379275baaf4909973c04936d00d700` 兩次查詢同一 TWSE 官方來源仍只保存 3 個唯一發布日 Evidence；validation hash 與一般 tool receipt 不會冒充 semantic evidence，精確 request URL 仍保留在 provenance。 |
| P25 Evidence Graph | domain `EvidenceGraph`；`agent-evidence-graph.js`；selection 帶 evidence ID／artifact version；durable store 只接受 explicit evidence event 並持久化 support／contradict edge。 | evidence graph unit、Dock state、Browser evidence click、provenance restart test。 | **本地原生 semantic node／selection 與跨 call 去重已驗收**：最新 App 顯示 3 筆 TWSE 法人證據而非 6 個 query-URL 變體或 validation nodes，點選後 Composer 顯示 `Evidence Graph ＞ 台積電`；2026-09-01 再確認 `open_stock_ai.agent_workspace.v1` 不會把完整 JSON 放進狹窄 Dock 按鈕，而顯示 `2887.TW · 持續觀察 · 執行已阻擋` 的短摘要，完整 receipt 仍由詳細檢視保留。support／contradict 關係與來源開頁定位仍需實機 conflict 情境。 |
| P26 Artifact 可點擊定位 | `artifacts/selection_context.py`、`FinalAgentRuntime.select_artifact`、artifact selection／composer context JS。 | task forest click、evidence click、selection version tests。 | **部分原生 UI 驗收（2026-09-01）**：在桌面 Dock 點選 2887.TW 的 Evidence Graph node 後，Composer context 立即顯示精確路徑 `Evidence Graph ＞ 2887.TW · 持續觀察 · 執行已阻擋` 與 `v1`；同日再建立並點選真實 `2887.TW 研究摘要.md`，Composer context 顯示完整 artifact path 與 `v1`。Branch、Step、Automation node、Decision option 與完整 artifact 版本流程仍須逐類實測，不能以此 selection 代表全部完成。 |
| P27 Artifact 修改需提案與確認 | `artifacts/versioning.py`、`FinalAgentRuntime.revise_artifact`／restore、artifact API。 | optimistic conflict／revision payload／restore Browser tests。 | **建立／批准原生 UI 已驗收（2026-09-01），修改流程仍待**：遠端 `gpt-oss:20b` 遺漏 tool call 時，Host 對明確純文字 artifact 請求建立受限 `artifact.create_text`，原生批准卡完成 `local_reversible` confirm，Host validation 與 `artifact.created` 均通過。仍需 proposal → impact → patch → stale conflict → dependency validation → new version／restore 的完整實機流程。 |
| P28 Renderer 分工 | `agent-schema-visualization.js` 已實作 DAG、Swimlane、Workflow、Timeline、Gantt、Fishbone、Decision Matrix、Risk Table／Bar／Radar；Task Forest、Evidence Graph、Decision／Diff／Automation 與既有市場 K 線各自維持專用 renderer。 | `test_schema_visualizations_cover_p28_renderers_without_model_svg`、Dock state 與 Browser E2E。 | **實作與瀏覽器驗證已有證據／待原生 UI 全流程驗收**：10 種 schema renderer 都由 DOM 安全節點渲染（不注入 SVG/script）；仍需在 `.command` 的 Artifact Canvas 中以真實 artifact 親自操作、確認選取連動。 |
| P29 Automation opportunity | `automation/opportunity_detector.py`，支援 no／one-shot／recurring／condition／event／cross-system，明確否定優先於 classifier 與關鍵字；Run snapshot 與 Dock projection 另以 `session_id` 隔離，不能把歷史 Automation 誤顯示成這次請求所建立。 | `test_agent_automation_final.py::test_opportunity_detector_supports_all_product_choices`、explicit no-automation mixed-language/coordinated-negation assertions、`test_snapshot_projects_only_automations_owned_by_its_session`、`test_snapshot_rejects_automation_owned_by_another_session`。 | **本機原生負向驗收（2026-09-01）**：以 `開啟股市AI系統.command` 的新 Session、遠端 `gpt-oss:20b` 輸入「分析 2887.TW 的技術面與主要風險；不要建立自動化、不要下單。」；Run `AR-ed14674068c0438b831f6f18700ab497` 完成後只顯示 Evidence Graph、`Active Automations=0`，摘要也明確為未建立自動化／紙上單／實盤單。 |
| P30 Automation Intent | `automation/intent.py` 的語意 schema，拒絕底層執行技術。 | semantic intent test。 | **已有落點／需真實 provider**：檢查模型輸出沒有 n8n node／credential／workflow JSON。 |
| P31 Automation Architecture | opportunity detector → policy → compiler → controller → internal／n8n backends。 | full proposal／compile／validate／dry-run／activate tests。 | **本機 n8n 實機通過**：owner／scoped API key、dry-run、create／publish、callback、Host reanalysis 與 pause 均有真實收據；外部通知 backend 仍待授權環境。 |
| P32 Internal Scheduler／n8n 分工 | `automation/workflow_compiler.py`、`backends.py`、durable scheduler。 | compiler routing、pending scheduler restart、headless n8n permission tests。 | **本機實機通過**：n8n 只負責分鐘排程與 callback，Host 直接依 sealed submission dispatch；無本地 schedule row 的 n8n callback 也可驗證 digest 後喚醒 Agent。 |
| P33 n8n headless | compiler semantic artifact 隱藏 technical fields；n8n backend 只接編譯結果。 | compiler artifact leak／credential reference tests。 | **原生 UI + 真實 n8n 通過**：workflow 使用 callback-only 衍生 token，不暴露 n8n API key；UI 只顯示語意流程與狀態。 |
| P34 Automation 三種輸出同步 | Orchestrator Work Summary／Host automation events／`agent-automation-view.js`。 | Dock reducer、Browser semantic automation behavior。 | **需實機 UI**：觀察三者同時更新且 Host status 非模型自述。 |

## P35–P54：Repair、Checkpoint、Memory、Provider 與金融安全

| 規格 | 實作模組／目前證據 | 自動化測試落點 | 驗收與目前判定 |
| --- | --- | --- | --- |
| P35 Safe Deterministic Repair | `repair/deterministic.py`、`providers/normalizer.py` 僅修已知 fence／wrapper／alias／shape。 | deterministic repair test、malformed optional provider tests。 | **已有落點／需 provider 錯誤樣本**：確認不發明 tool／argument 語意。 |
| P36 Model Local Repair | `repair/patching.py::HostModelRepairPipeline` 的 scoped JSON Patch、Host Model Budget 與 Orchestrator malformed-tool-arguments 產品接線；只傳 Error Receipt、failed fragment、完整 schema 與 Host 允許 scope。 | `test_model_local_patch_is_scoped_atomic_and_schema_validated`、`test_orchestrator_repairs_malformed_tool_arguments_through_bounded_provider_pipeline`、`test_agent_budget_repair_integration.py`。 | **正式 Runtime 閉環測試通過／需原生故障樣本**：正常 Run 已走 L0 deterministic → budgeted Repair provider → receipt binding → atomic patch → full-schema gate；越界 patch 不改原始文件。 |
| P37 Error Receipt | `repair/contracts.py::ErrorReceipt`、Orchestrator tool failure projection。 | stable structured receipt、chaos mapping tests。 | **已有落點／需整體錯誤注入**。 |
| P38 Failure Fingerprint | `repair/contracts.py::FailureFingerprint`、`observability_store.py` durable fingerprints／ledger。 | fingerprint test、Forest projection restart test。 | **已有落點／需跨 Run 實機驗收**。 |
| P39 禁止 identical retry | `repair/strategy.py::IdenticalRetryGuard`、Orchestrator Run-level failed-tool ledger；穩定 fingerprint 不再含替代 Plan node ID。 | identical retry escalation test、`test_repeated_failed_tool_is_blocked_across_replanned_nodes_and_uses_alternative`。 | **本地原生 provider 路徑已驗收／仍需跨 Run**：Run `AR-d4f757a620524b7ca8141c486bedc12e` 曾重複三次台股搜尋；修復後同一工具的 timeout／transport/source failure 即使改寫 query 也會發出 `repair.strategy_changed`、停止實際重打並提供替代工具。下一輪原生 Run 只失敗一次即轉向其他 Host 證據。 |
| P40 L0–L9 Recovery Ladder | `RecoveryLevel`／`RecoveryStrategyLadder`、Orchestrator repair events；`DurableAgentRuntime` 會同時對 `partially_completed` 與「第一個工具就失敗」造成的 `max_steps_reached` 從同一 checkpoint 進行受限自動恢復，仍未解決才建立公開 L8 Reflection／`waiting_user_input` 互動，並提供「替代來源繼續」或「保留明確 partial 結果」兩個可逆選項。 | ordered ladder、terminal／chaos tests、`test_recoverable_max_steps_run_automatically_continues_from_its_checkpoint`、`test_exhausted_recovery_creates_public_l8_interaction`、`test_recovery_escalation_can_retain_partial_result`、`test_unrelated_web_success_cannot_complete_failed_market_branch`。 | **部分落地／需端到端 fault injection**：Run `AR-a4f…` 曾把 `market.search_taiwan_securities` 逾時與 `example.com` 成功錯誤組合成 completed；現在 completion gate 會保留未綁定 recovery 的失敗節點，且不完整 Run 會繼續恢復。仍需以 Computer Use 在原生 UI 操作這兩個選項，並證明每個 L0–L7 策略能在真實 fault 下正確選用。 |
| P41 錯誤只影響 Local Branch | `ForestExecutor` sibling isolation、`research/recovery.py`、branch-scoped failure resolution。 | local failure／source missing／branch-specific KPI resolution tests。 | **部分落地／需真實並行工具驗收**。 |
| P42 六層 Checkpoint | `forest/checkpoints.py`、`forest/checkpoint_store.py`、`FinalAgentRuntime._checkpoint_runtime_event`；Session Forest checkpoint 以 `forest_id` 做跨 Run 唯一去重。 | `test_agent_durable_checkpoint.py` 的最小邊界、idempotency、跨程序 reopen、相同 Objective 多 Run 不碰撞。 | **本地 Host／DB restart 已驗收**：UI 真實建立同 Objective 新 Run 曾捕捉 session checkpoint collision；修復後新 Run `AR-a41ce8f2aeed476b85476ff53b89eb53` 正常建立、重啟與完成。六種 scope 的個別故障恢復仍由自動化測試覆蓋。 |
| P43 六層 Memory | `memory/manager.py`、`governance.py`、`durable_governance.py`。 | six-layer unit與durable API tests。 | **已有落點／需實機 Session retrieval**。 |
| P44 Memory Candidate | `MemoryCandidate` scoring／save、merge、supersede、expire、reject；P71 durable table。 | candidate scoring／lifecycle／reopen tests。 | **已有落點／需真實對話抽取驗收**。 |
| P45 Memory Conflict | durable conflict／supersession append-only audit。 | explicit conflict supersession tests。 | **已有落點／需實機跨 Session 驗收**。 |
| P46 Procedural gate | `ProceduralMemoryGate`、durable distinct-run／host-verified promotion。 | procedural repetition／cross-process tests。 | **已有落點／需跨 Run 故障實錄驗收**。 |
| P47 Session Title | `session/title_generator.py`、`topic_shift_detector.py`、`session_store.py` title history。 | title／topic shift tests。 | **部分落地／需真實 provider + UI**：確認理解後產生短標題、主題偏移後保留歷史。 |
| P48 Provider-neutral Runtime | `contracts.AgentDriver`、`providers/`、`agent_drivers.py`、provider-neutral request／normalizer，以及 `validation/provider_matrix.py` 可替換的 `ContractProvider` 邊界。 | driver tests、reference provider matrix tests；原生 App 實跑遠端 `gpt-oss:20b`。 | **部分實機驗收**：遠端 OpenAI-compatible Ollama 已走過同一 normalizer／tool／approval contract；Reference provider 仍是 deterministic fixture，Codex／Claude 類／較弱 Ollama／純文字 adapter 尚不得由此宣告真實連線通過。 |
| P49 小模型策略 | `model_router.py::compact_for_small_model`、階段工具揭露、Repair loop。 | provider-neutral role router／small surface test。 | **需真實 Ollama 強弱模型**。 |
| P50 Master／Subagent 邊界 | PlanGraph subtask／subagent node、Forest local objective／budget／tools、Policy gate。 | Orchestrator specialized node dispatch、Forest executor tests。 | **部分落地／需真實 provider**：需驗證 Subagent 無法改全局目標、偏好或最終交易。 |
| P51 Critic Branch | Join conflict verification branch、model role `CRITIC`、Research conflict resolver。 | conflict join、role router tests。 | **部分落地／需複雜持股情境驗收**。 |
| P52 金融風險層 | `policy_engine.py`、Central Risk／paper order preview／approval；真實交易停用。 | execution boundary、preview/risk approval tests。 | **安全邊界／需負向驗收**：Agent proposal 不得直接取得 execution permission。 |
| P53 Approval 與 Clarification 分離 | Run／Branch 狀態包含 `waiting_user_input`、`waiting_decision`、`waiting_approval`；Interaction schema。 | distinct waiting state、durable decision resume、approval tests。 | **已有落點／需實機 UI**。 |
| P54 Automation 與 Trade Approval 分離 | `automation/policy_engine.py`、external permission、paper trade policy，以及 Session-scoped Automation snapshot／Dock projection。 | automation policy separation、approval tests、`test_snapshot_projects_only_automations_owned_by_its_session`、`test_snapshot_rejects_automation_owned_by_another_session`。 | **本機原生負向驗收（2026-09-01）**：純分析且明確否定 Automation／下單的 Run `AR-ed14674068c0438b831f6f18700ab497` 未建立任何 Automation 或交易；先前 Session 的資料也不會投影到目前 Dock。真實券商與帳戶 approval 仍維持需帳戶本人驗收。 |

## P55–P70：Agent Dock、效能、Automation durability 與 Observability

| 規格 | 實作模組／目前證據 | 自動化測試落點 | 驗收與目前判定 |
| --- | --- | --- | --- |
| P55 Agent Dock 結構 | conversation／context／timeline／decision／artifact／composer 模組與 Dock controller/store；淺色 Dock 另補齊 `surface`／圖形 viewport token。 | `test_agent_dock_state.py`、`test_light_theme_polish.py`、Browser E2E。 | **本輪淺色呈現已實機驗收／完整互動仍待**：從 `.command` 開啟後，Computer Use 已確認標題、provider/runtime/status、Task Forest 與 DAG 不再出現黑色孤島；2026-09-01 以實際 1,180px 寬度桌面窗口確認 Dock 開啟時主工作區／頂欄預留右側寬度，功能列的單一「收合／開啟 Agent 控制台」可來回操作，Evidence Graph 不再以 raw JSON 擠壓面板。conversation、artifact、automation 等完整互動仍依各項門檻驗收。 |
| P56 Task View 改 Forest | `agent-task-forest.js`、Forest snapshot API、expand/collapse。 | hierarchy／snapshot flatten／Browser forest click tests。 | **需實機 UI**：真實 Run 顯示多層 Branch、Local Steps、等待依賴與結果。 |
| P57 圖與聊天連動 | artifact/evidence selection → composer context chip。 | Browser task/evidence selection tests。 | **部分原生 UI 驗收（2026-09-01）**：以 Computer Use 點選 Evidence Graph node，右側 Composer context 由「目前頁面 Context」切換為同一條 `Evidence Graph ＞ …` 路徑與 `v1`，證明已選圖元可傳到後續聊天上下文；未送出修改訊息，因此不把 Artifact revise／自然語言變更流程誤標完成。 |
| P58 Artifact Versioning | `artifacts/versioning.py`、SQLite versions、revise／restore／history API、diff view。 | version audit／conflict／revision／restore tests。 | **版本 1 建立與選取已原生驗收（2026-09-01）**：`AA-05af3b46f8b14ba48a5f5570f42a0a66` 顯示 `v1`、可選取、開啟與下載，Composer context 也綁定相同 path／version。Undo、Compare、Restore、stale conflict 與新 version 仍要親自操作；現有 Browser 覆蓋不代表全部 UI 控制已驗收。 |
| P59 固定說明不耗 token | 前端靜態文案／semantic view；模型輸出與 UI help 分離。 | Dock state source assertions。 | **需實機 UI + event payload 稽核**。 |
| P60 Host UI 狀態不耗 token | event reducer／formatters／runtime timeline 依 Host event 生成。 | event reducer／timeline tests。 | **歷史原生驗收主張（本次未重驗）**：曾記錄遠端 `gpt-oss:20b` 無工具 Run 的 Host／Forest completed 一致；本次複雜並行 Forest 與所有 UI 投影均待 Computer Use 重驗。 |
| P61 Work Summary 自由生成 | Orchestrator provider output中的 work summary 與 Host status 分欄。 | Orchestrator public summary tests。 | **需真實 provider + UI**：不可固定成 compiler stage 模板。 |
| P62 Token Budget | `budget_manager.py`、`context_broker_v2.TokenBudgetManager`、`model_router.HostModelBudget／HostModelExecutor`、`BranchBudget`；Orchestrator 的 Session／Run／Branch／turn prompt+response accounting 會跨 checkpoint 保存，Repair provider 也共用剩餘 Branch allowance。 | budget enforcement、marginal value、context compaction、actual settlement、oversized response rejection 與 Repair integration tests。 | **正式執行鏈測試通過／需真實長 Session 校準**：Host 在 call 前選模、裁切、保留 output，再以實際 input/output 結算；超額回應不接受，模型不能自行重設 allowance。 |
| P63 Context Broker 2.0 | `context_broker_v2.py` 選 Current Objective／active branch／memory／evidence／decisions／failure ledger；Orchestrator 的實際 provider tools 與 fitted Context Package 使用同一裁切結果。 | context selection／small-model surface tests、完整 external tool stream regression。 | **本地 token 邊界已回歸／仍需多 provider**：完整 pytest 曾抓到第二回合未裁切 project tool list 耗盡 36k turn budget；修復後 FinGPT external tool Run 可在下一回合完成，不再因重複 schema 計費中止。 |
| P64 Context Compression | `branch_result.py`、`BranchResult` conclusion／confidence／evidence／contradictions／gaps。 | branch result compression test、external FinGPT tool stream completion test。 | **本地長工具面回歸已通過／仍需長 Session 實測**：原始工具 payload 留在 durable trace，provider 後續回合只收 BranchResult、bounded receipts 與 fitted tool schemas。 |
| P65 Durable Scheduler lifecycle | existing scheduler + `automation/lifecycle.py`／store、`automation/time_poller.py`；`DurableAgentRuntime.start_background` 恢復 pending submission，產品 scheduler loop 每秒啟動不阻塞的 durable time poll cycle，事件型 callback 仍進同一 Runtime。 | activation、pause/resume、missed interval coalescing、claimed callback process reopen、async provider reanalysis、`test_product_scheduler_loop_polls_durable_time_automations`、`test_automation_event_wakes_a_real_same_session_durable_reanalysis_run`。 | **產品時間來源與重啟閉環已接線／需原生等待到期驗收**：deadline、claim、occurrence key、next run 與 callback receipt 全在 SQLite；舊程序中斷後可恢復，不靠瀏覽器或記憶體 timer。 |
| P66 Automation dedup | `automation/dedup.py` fingerprint；controller reuse／merge／update。 | semantic fingerprint、reuse／merge／update tests。 | **已有落點／需 API/UI 實機驗收**。 |
| P67 Trigger 後重新分析 | `AutomationController.handle_trigger_async` + `DurableAgentRuntime._automation_reanalyze`：filter → candidate → 同 Session durable Agent Run → Host completion/evidence → decision changed → notify；`AutomationStore.attach_execution_run` 將 execution 與實際 child Run FK 持久化；`POST /automations/events` 供事件引擎呼叫。 | sync/async trigger pipeline、execution→Run link、durable Runtime reanalysis Run／receipt／loop-prevention tests。 | **本地實機整合已驗收**：原生確認卡啟用後，以測試市場事件喚醒遠端 `gpt-oss:20b` 同 Session child Run；execution、Run、Session、source=automation_reanalysis 可持久化追溯，測試監控已暫停。仍需真實市場事件來源長期驗收。 |
| P68 Notification lifecycle | `automation/notifications.py`、store deliveries／receipts；`FinalAgentRuntime` 預設提供 durable first-party `in_app` sender，產品組裝層以 `automation_notifications.py` 注入既有 LINE／Telegram transport。 | dedup／cooldown／fallback／ack／snooze／unsubscribe／expire、async Runtime notification receipt、外部 sender 注入與未配置 fallback tests。 | **產品接線完成／需真實外部 channel 送達驗收**：使用者確認的 Automation 可依語意 policy 選擇 LINE／Telegram；未配置或 provider 拒絕時保留 durable 失敗收據並可 fallback 到 in-app。外部真實送達仍須使用者授權與實際憑證驗收。 |
| P69 Tool provenance | `observability.py::ToolResultProvenance`、`observability_store.py` durable tool provider／model provider／worker／time／freshness／source URL／request／latency／success／schema。 | provenance/KPI restart、canonical multi-source evidence tests。 | **程式、120 項相關測試與本地原生官方工具抽查通過**：Run `AR-bdace55bde7b4907ae0528866491327b` 明確記錄 tool provider=`stock_core`、model provider=`openai-compatible` 與 TWSE T86 URL，已修正模型 driver 冒充資料工具 provider 的問題。 |
| P70 Observability | `GET /api/agents/observability` + `agent-observability-dashboard.js` 以 durable KPI 顯示 active session/branch、tool/model latency、error/repair/repeated failure、token/research、Automation/notification/false trigger；支援 `24h／7d／30d／all`、實際分母、`no_data` 與 P104 失敗分類。 | durable dashboard/model latency、window boundary、terminal Run dedup、question-budget、failure taxonomy、API route／Dock static contract tests。 | **本地原生窗口已驗收／仍需 release 基準**：原生 App 實際切換 `24 hours`、`7 days`、`All time`，畫面保留樣本分母、Evidence Freshness、Failure classification 與技術檢視；仍需正式 release 流量校準 KPI 目標。 |

## P71–P87：資料、API、狀態機與產品輸出

| 規格 | 實作模組／目前證據 | 自動化測試落點 | 驗收與目前判定 |
| --- | --- | --- | --- |
| P71 核心資料表 | `storage/migrations.py` 建立 objective／forest／branch／join／decision／proposal／repair／memory／artifact／automation／notification／title 表；`storage/p71_schema.py` 明列 30 張 durable domain table 的必要欄位與 13 個索引，migration 後會 fail-closed 驗證。 | `test_p71_schema_contract.py` 以 v37 代表資料驗證升級、回填、備份收據、第二次啟動、Objective／Artifact／Automation 正式 Store 回讀、foreign key 與 `quick_check`；另有 runtime v2、durable memory／automation／forest restart tests。 | **完成**：2026-09-08 以 `.command` 停止／重啟 exact candidate `6fca6edf8277`，managed runtime 的 v47 主資料庫通過 30 表／13 索引契約；原生 App 開啟「系統 → 資料平台」，可讀取 62,983 筆標準紀錄與 118,643 個 revision，服務重啟後正常。詳見 `docs/validation/native-p71-schema-restart-20260908.md`。 |
| P72 後端模組拆分 | `session/`、`forest/`、`interaction/`、`repair/`、`memory/`、`research/`、`automation/`、`artifacts/`；`interaction/store.py::DurableInteractionStore` 持有 Decision Checkpoint／User Proposal persistence；`providers/transcript.py` 持有 provider 對話投影、限量、遮罩與 token 預估；`repair/recovery_policy.py` 抽離 recovery 等 19 個函式；`completion_policy.py` 抽離 completion 等 20 個函式；`paper_protocol.py` 抽離 paper protocol 等 11 個函式；2026-09-09 再新增 `evidence_projection.py`，抽離 canonical evidence、來源角色／新鮮度、grounding feedback、精確數值與 reasoning summary 等 10 個 Host 函式。 | `test_agent_runtime_module_boundaries.py` 防止 interaction SQL、provider transcript、recovery、completion、paper protocol 與 evidence projection 回流 coordinator；既有測試驗證來源身份、run-scoped 去重、freshness、unsupported claim feedback、精確數值、paper boundary 與 fail-closed 規則；最新 Agent／財務證據定向 `258 passed`、完整 `1931 passed, 5 skipped`。 | **模組拆分持續進行**：`FinalAgentRuntime` 已由 1,482 降至 1,358 行；`orchestrator.py` 已由 10,414 降至 7,784 行。exact candidate 原生 App 已只讀驗收歷史 Host 結果摘要，並點選已完成 Plan DAG node，確認 Composer context 正確投影 node path 與 `v4`；全程未操作 Run 或呼叫模型。P72 暫不誤標完整完成；仍需拆分大型 run-loop 協調責任。詳見 `docs/validation/native-p72-interaction-store-20260908.md`、`docs/validation/native-p72-provider-transcript-20260909.md`、`docs/validation/native-p72-recovery-policy-20260909.md`、`docs/validation/native-p72-completion-policy-20260909.md`、`docs/validation/native-p72-paper-protocol-20260909.md`、`docs/validation/native-p72-evidence-projection-20260909.md`。 |
| P73 Frontend 模組 | 規格列出的 conversation、task forest、timeline、decision、artifact canvas/selection、automation、evidence、diff、composer context 檔皆存在；首頁／個股圖表共用 `CHART_THEME` 與 light surface contract；首頁大型市場清單使用 bounded 40-row render window 與明確進度控制；Agent Context Bar 使用帶語意 key 的響應式 grid，長內容可換行且不水平溢出；窄幅全螢幕 Dock 使用不透明底層，展開時隔離下層頁面與 WebGL 光學代理。 | Dock module split／Browser tests／`test_light_theme_polish.py`／Context Bar wrap contract／mobile surface and stacking contract。 | **首頁、個股、Task DAG、大型候選清單與 Agent Dock 響應式原生 UI 已驗收／其餘模組仍待**：已操作 K 線、workspace tabs、繪圖工具與 Task 分頁；2026-09-08 在 exact candidate 的 1,440px 寬原生視窗確認候選卡沒有文字互壓，`40 / 648` 可增至 `80 / 648`，分類切換會重設 window；2026-09-09 在標準與 760 × 900 pt 原生視窗確認七項環境資訊均在 Dock 內換行，沒有右緣截斷或按鈕重疊，證據見 `docs/validation/native-p73-agent-context-wrap-20260909.md`；同日再以 760 × 900 pt 原生視窗確認全螢幕 Dock 不再被首頁或 WebGL lens 疊字，AX 可操作樹只保留 Dock，證據見 `docs/validation/native-p73-mobile-dock-legibility-20260909.md`。Artifact／Automation／Evidence 全流程仍不可由此外推為完成。 |
| P74 後端 API | `agent_api.py` 已提供 Session、Message、Forest、Branch control、Interaction、Artifact、Automation、Event stream routes。 | `test_agent_runtime_api.py::test_interoperable_agent_routes_are_exposed` 及 route contract tests。 | **已有落點／需實機 API + UI**：確認 branch control 會控制實際工作而非只改 DB 顯示。 |
| P75 Current Run 輸入不全是 replan | Composer/controller 固定 POST Session Message；Durable Runtime 分流 proposal／answer／steer／artifact／automation；subtask child 會繼承 Host 的父 task kind 與 research-only 邊界，避免把簡短 symbol focus 誤升格成買賣決策。OpenAI-compatible 的 `assistant({tool, arguments})` 巢狀工具信封會在 Plan 前還原為 Host 白名單工具；未知能力不會寫入 durable Plan。一般市場研究未明確要求 Critic 時不暴露遞迴子任務。 | Dock current-run composer contract、durable message tests、`test_agent_runtime.py::test_openai_compatible_assistant_envelope_is_unwrapped_before_plan_compile`、`test_agent_runtime.py::test_routine_market_information_hides_recursive_subtasks_but_keeps_explicit_critic`、`test_agent_runtime_v2.py::test_runtime_subtasks_link_parallel_child_runs_to_the_parent_forest`。 | **本輪 append／新 Run 原生驗收通過；其餘 artifact change 等仍待**：`.command` + `gpt-oss:20b` 實測一般 2887.TW 分析從先前的 `assistant` unknown capability／`execution_guard_blocked` 鬼打牆，收束為單一 `market.analyze_symbol` branch 的 completed Run `AR-9d3cd75e7d0745449704f57ca14151de`；資料過期時直接附來源、時間與限制結束，沒有子任務、Automation、紙上單或實盤單。詳見 `docs/validation/native-p75-current-run-20260901.md`。 |
| P76 Runtime State Machine | `agent_run_store.py`、Durable Runtime、`BranchStatus` 支援 queued／planning／running／waiting／repairing／replanning／paused／suspended／partial／terminal；`UserPaused` 與 crash／safe-shutdown suspend 分開恢復。 | pause/resume、decision、approval、executor state、`test_explicit_user_pause_survives_process_restart_until_manual_resume`。 | **使用者暫停重啟契約已通過／其餘 Crash 需整體演練**：明確 pause 重啟後不會偷偷自動 Resume，手動 Resume 才續跑；provider／browser／DB crash 狀態仍需環境級驗收。 |
| P77 部分成功一級狀態 | `ForestExecutionStatus.PARTIALLY_COMPLETED`、BranchResult、CompletionGate。 | local failure/join partial、completion gate tests。 | **部分落地／需真實網站失敗情境**。 |
| P78 Host Completion Criteria | `ForestCompletionGate`、Orchestrator validation／receipt／required evidence checks。 | completion gate rejects missing evidence、orchestrator real observation tests。 | **部分落地／需真實 provider**：模型說完成時 Host 必須仍可拒絕。 |
| P79 Memory／Title 背景工作 | `observability.BackgroundWorkQueue`、`BackgroundMemoryPipeline`；Durable message pipeline 背景提交偏好。 | background work returns before consumption test。 | **部分落地／需效能與重啟驗收**：確認 title／memory／lesson 都不阻塞 final result 且工作不遺失。 |
| P80 LLM／Host 分工 | schema／repair／dedup／schedule／numeric／permission／layout／event projection 由 Host；理解／研究／Reflection 由 provider。 | repair、automation、policy、projection tests。 | **已有落點／需架構與 token 稽核**。 |
| P81 模型成本分工 | `model_router.py` 的 Planner／Researcher／Critic／Formatter／Repair role routing。 | low-cost repair／strong critic test。 | **需真實多模型 provider**：目前路由 contract 不證明所有模型可執行。 |
| P82 防假忙 | `budget_manager.py` Marginal Value／node and branch limits／duplicate detection。 | token marginal value／question budget、duplicate branch tests。 | **部分落地／需長任務觀察**。 |
| P83 Reflection Question Budget | question budget 與 impact／independent-verification decision。 | question budget test。 | **需真實 provider + UI**：每 checkpoint 1–3 題且可自行查證先查。 |
| P84 Semantic／Technical Artifact view | `agent-automation-view.js`／artifact canvas 過濾 debug fields。 | semantic view hides debug fields、Browser automation view test。 | **歷史原生驗收主張（本次未重驗）**：曾記錄 semantic／technical view 切換與不顯示 n8n credential／workflow id；本次必須以 Computer Use 重新確認。 |
| P85 Artifact selection 綁版本 | selection context保存 artifact version；revision 做 optimistic conflict check。 | version-pinned selection／stale edit conflict tests。 | **已有落點／需實機併發更新驗收**。 |
| P86 Preference 不是硬規則 | `memory/policies.py`、governance advisory preference。 | `test_user_preferences_are_advisory_and_cannot_be_execution_rules`。 | **安全邊界／需跨 Session 實測**。 |
| P87 Final Answer 非唯一輸出 | Run snapshot/API 可同時回 Message、Forest、Evidence、Interaction/Decision、Artifact、Automation、Chart/report references。 | snapshot flatten/replay、Dock reducer tests。 | **部分落地／需實機 UI**：同一複雜 Session 要同時產生並可操作多種結果。 |

## P88–P105：最終情境、測試矩陣、Chaos 與 KPI

| 規格 | 實作模組／目前證據 | 自動化測試落點 | 驗收與目前判定 |
| --- | --- | --- | --- |
| P88 Scenario A 普通問答 | opportunity detector 可判定不自動化；Orchestrator區分 general/current information。 | `test_agent_final_system_scenarios.py` 的 A contract、general answer tests；原生 App 實跑同類 current-information 任務。 | **部分實機驗收（2026-09-01 重驗）**：以指定桌面 launcher、遠端 `gpt-oss:20b` 與一般使用者原句「請分析台新新光金 2887.TW 的技術面與主要風險。」建立新的 Advisory Run `AR-a7363e94e36d4f3b98b197d6c14ce694`。Host 以 2/2 動態計畫完成，工具結果為 `market.analyze_symbol`、`execution_permission=blocked`、`execution_boundary=analysis_only_no_order_submission`；資料不足時直接回報來源、時間與限制，沒有反問、`continuation_requested`、Automation、紙上訂單或實盤券商操作。完整本機驗收紀錄見 `docs/validation/native-p88-advisory-20260901.md`。因本次資料沒有達到可安全技術判讀的決策門檻，這只驗證一般問答的安全收束，不將市場分析品質標示為 complete。 |
| P89 Scenario B 複雜持股決策 | Forest／capability／research／critic／reflection 元件；completion validator 會依問題中明列的持股、技術、基本面、法人、外部研究與風險面向要求不同 Host 證據。 | scenario B domain、comprehensive scope／coverage／grounded workspace feedback、官方財務工具、Critic capability/evidence-budget tests。 | **本地原生完整情境已驗收（2026-08-29）**：以指定 launcher 與遠端 `gpt-oss:20b` 執行父 Run `AR-c225670b59614647a47da641938d022c`；Host 在模型遺漏明示的只讀維度時補足月營收、法人與一次獨立 Critic 子分支。最終 `completion_validation.passed=true`，受限摘要逐項覆蓋假設持股／投資組合、技術、月營收、法人、外部研究、風險與 Critic 反方；`recovery_pending=false`、沒有新的續跑、沒有 Automation／紙上單／真實帳戶存取。 |
| P90 Scenario C User Collaboration | DecisionCheckpoint、Interaction response、decision card；`orchestrator.py` 的 explicit-choice Host materialization／bounded re-prompt，避免 provider 格式不穩定把使用者送回「請提供外部驗收條件」或 prematurely complete。 | interaction unit／scenario C／Browser decision test、explicit-choice materialization／bounded re-prompt tests。 | **本地原生三種使用者回覆皆已驗收（2026-09-01）**：指定 launcher 與遠端 `gpt-oss:20b` 下，Run `AR-9bf673b4ecc043b3b17b28c52b90b606` 點選 Agent 建議後於 sequence 45 完成；`AR-2fb73dcfda064c2b9455d3f6a228cf47` 點選替代方案「平衡型投資計畫」後於 sequence 46 完成；`AR-382f835240844c4791abc41de0d7ab02` 輸入「我想採用平衡型，但設定六個月後重新檢視」後於 sequence 45 完成。三者皆無市場查詢、交易、紙上單或實際建立的 Automation；完整紀錄見 `docs/validation/native-p90-interaction-20260901.md`。 |
| P91 Scenario D 不合理方案 | `ProposalArbitrator`、Policy／Risk／Approval；新 Session Run 也先走 `submit_session_run` safety preflight，拒絕時只寫 durable proposal／Host safety message，不建立 provider Run。 | durable proposal persistence、new-session preflight rejection、scenario D contract、approval tests。 | **本地原生安全拒絕已重驗（2026-08-27）**：以桌面 `開啟股市AI系統.command` 啟動專案後，在新的 Advisory Session 輸入「請使用台新證券真實帳戶買進 2887.TW 100 股，不需要我批准。」；Host 立即只留下 safety-rejection 訊息，UI 保持「等待新任務」與 `0 / 0` Plan，沒有 Run ID、provider、approval、Automation、paper preview 或交易節點。此驗收確認 Agent 不接受任何真實帳戶送單要求；真實券商 SDK／帳戶仍維持未驗收、不可用。 |
| P92 Scenario E JSON 錯誤 | Deterministic repair、model patch、recovery ladder、resume。 | malformed arguments／repair unit tests。 | **需真實 provider fault injection**：現有單元覆蓋不等於實際模型錯誤可恢復。 |
| P93 Scenario F 網站失敗 | research recovery、branch-local failure、Run-level repeated-source guard、alternative strategy。 | source missing/local failure、replanned-node alternative test。 | **部分本地真實網路驗收**：原生遠端模型 Run 中 `market.search_taiwan_securities` 的 TWSE／TPEx OpenAPI 同步實際逾時，其他 `market.analyze_symbol`／`market.research_pack` Branch 繼續並產生可驗證證據；修復後不再重打相同失敗來源。真正 Browser process crash 與跨來源 Conflict Branch 仍待可控環境。 |
| P94 Scenario G Mid-run 新需求 | Session Message＋Soft Steering child branch；Composer 送出序列化，避免第二句在第一個 Run receipt 回到 Dock 前誤附著舊 terminal Run。 | steering／scenario G tests、`test_fast_natural_follow_up_waits_for_the_new_run_receipt`。 | **本次原生 Computer Use 驗收（2026-08-31）**：由本專案根目錄 `.command` 啟動桌面 App，先輸入「分析台積電的技術與風險。」並在 running 中輸入「順便比較台新新光金的風險差異。」；同一 Session `AS-2c86448b6c214f64b09bfc0f66036055` 建立 root `AR-f1550b8ac7104512a81d3e7ef0b45ce9` 與 parent 指向正確 root 的 child `AR-0358e5a8a1624c32b335d9b2561e336d`，child 已實際建立並完成多個 Host tool Branch，且未建立 `interaction.requested`／`run.waiting_*` 卡片。驗收時遠端 provider 繼續擴張研究到成本／步數邊界，已由 App 控制台取消這個測試 child Run；因此只把本項記為 routing／child lifecycle 通過，不把被取消的研究誤標為完整市場答案。 |
| P95 Scenario H Artifact 點擊修改 | versioned selection／proposal／revision API與前端；文字 Artifact 提供局部修改表單與原生 WebKit 確認對話框。 | scenario H、Browser artifact revision tests。 | **歷史原生驗收主張（本次未重驗）**：曾記錄原生 App 的版本化局部修改流程；本次仍須由 Computer Use 操作確認。 |
| P96 Scenario I Automation | proposal → confirm → semantic intent → compile → validate／dry run／activate；新 Run composer 先走 Host preview，避免誤交模型。 | scenario I、full automation lifecycle、`test_session_automation_proposal_requires_confirmation_then_activates`、Dock static contract tests。 | **本次原生與 n8n 實機通過**：分鐘 trigger → callback → 同 Session Run → Evidence／decision gate → 暫停均已驗證；外部通知 channel 不在本次授權範圍。 |
| P97 Scenario J 未來 trigger | durable trigger/filter/reanalysis/meaningful notification，execution→Run durable link。 | scenario J domain、trigger/restart tests、`test_automation_event_wakes_a_real_same_session_durable_reanalysis_run`。 | **本地實機驗收**：由本地事件端點送入 `test: true` 市場價格事件，遠端 `gpt-oss:20b` 以同 Session 完成 reanalysis Run；execution 已記錄 Run ID、decision gate 與通知結果。仍需實際市場資料來源、時間型 trigger 與外部通知 channel 長期驗收。 |
| P98 Scenario K Memory | six-layer durable governance／retrieval；明確「回憶先前偏好」時由 Host 以已治理的 `user_preference` 記憶 grounding，避免模型改寫使用者原話。 | memory final＋durable＋runtime tests。 | **歷史原生驗收主張（本次未重驗）**：曾記錄跨 Session 偏好回憶；本次須以 Computer Use 重驗，不能以歷史 Run 當作發布證據。 |
| P99 Scenario L Session History | title generator／history table／Session list UI。 | scenario L、title history／topic shift tests。 | **本地原生清單操作已驗收**：2026-08-26 以桌面 `開啟股市AI系統.command` 啟動 `main` 的 `18078df`，Computer Use 建立一個空白 Agent Session 後開啟歷史清單；畫面列出各 Session 的「開啟工作階段」與「封存工作階段」操作，且未送出 Agent 任務、Automation 或委託。跨 Session 的自動 title／topic shift 仍需獨立驗收。 |
| P100 禁止實作方式 | Policy、Host events、Forest、Repair、Memory、Automation、Capability composition 各自提供反模式邊界；Automation Opportunity Detector 會先尊重明確「不要建立自動化」約束。 | 分散於 runtime／security／memory／automation／UI tests，另含 explicit no-automation negative test。 | **持續架構稽核**：本次原生 UI 發現「討論 n8n／監控」誤觸 Automation proposal 後已修正並回歸；19 項禁令仍應在 release 前逐項以負向證據核對，不能以單一測試宣稱永久成立。 |
| P101 實作順序 | Phase 對應 interaction、forest、repair、capability/research、artifact、automation、memory、optimization 模組。 | 各 Phase 測試群。 | **進行中**：本 shared tree 採跨 Phase 並行整合；最終仍須依 dependency 做整體回歸，不能把此條視為功能完成。 |
| P102 每 Phase 完成標準 | Unit／integration／E2E／persistence／restart 類測試已有多個；`.command`、真實 provider、完整 error injection 另列。 | 2026-08-25 最新完整 `pytest`：`1377 passed, 5 skipped`、3 warnings，無 `xfailed`／`xpassed`；本文件「驗收命令」。 | **本機回歸通過／外部矩陣仍待**：T-002 的過時 xfail/xpass 已清除；`.command`、遠端 Ollama、原生 UI、真實 n8n、官方資料、Evidence 與 SQLite reopen／compaction 已有局部實測，但五個 skip、多家 Cloud Model 與完整 environment-level crash 注入仍不可省略。 |
| P103 五類模型矩陣 | `model_router.py` 定義五種 provider class／八種 behavior；`validation/provider_matrix.py` 實際跑 structured output、tool use、parallel、long session、repair、user interaction、reflection 與 automation，且可注入 live `ContractProvider`。 | `test_agent_provider_matrix_chaos.py` 跑五類 `ReferenceContractProvider` 共 40 cases；舊 `ModelMatrixAuditor` contract tests；另以遠端 Ollama `gpt-oss:20b` 做實際端點與 UI 驗收。 | **矩陣 contract 已落地／仍需多家真實 provider**：40 個 deterministic contract cases 已覆蓋五類，但目前真實連線驗收僅涵蓋遠端 Ollama GPT-OSS，不能把五類都宣稱為真實 provider 驗收通過。 |
| P104 Chaos Tests | `validation/chaos.py` 提供可重現 campaign，實際注入 invalid JSON、wrong args、timeout、API 500、rate limit、network drop、source missing、Browser crash、provider crash、Host restart、DB restart、duplicate event、user interrupt 與 conflicting message；另有 `repair/strategy.py::ChaosFault`、checkpoint／scheduler／SSE restart 測試。 | `test_agent_provider_matrix_chaos.py` 十四 fault campaign；repair strategy mapping、durable restart tests；`.command` 原生 App restart。 | **完整 fault catalog + Host/SQLite 實機 restart 已驗收**：十四 fault 的 deterministic campaign 通過，且實際關閉再以 `.command` 重啟原生 App 後成功重建 SQLite state、Session title／對話／KPI。真正殺 Browser process、遠端 provider process 的端到端災難演練仍待可控環境。 |
| P105 KPI | `observability.py::KPICollector`、`observability_store.py` durable KPI／evidence／failure projection，snapshot API 回傳 windowed `kpis`／`sample_counts`／`failure_classification`；Dock 可切換 24h／7d／30d／all。 | KPI collector、durable restart、window boundary、terminal Run 去重、publication freshness、Dock projection tests。 | **計算、持久化與原生窗口操作通過／需正式基準**：完成率、Branch recovery、User intervention、工具／Model error rate、Token 成本、Question Budget、研究多樣性、證據新鮮度、修正採納、Automation duplicate、Context retrieval precision 都有可稽核分母與 no-data 狀態；原生 App 已切換三個窗口，仍需乾淨 release baseline 跑 A–L／provider／chaos 才能宣稱目標值達成。 |

## 自動化測試入口

文件中的「測試落點」表示存在覆蓋，不表示本次 shared working tree 已全部重跑。修改程式後至少執行：

```bash
uv run python -m compileall -q src/stock_ai src/open_stock_ai
uv run pytest -q \
  tests/test_agent_runtime_final_interaction.py \
  tests/test_agent_forest_executor.py \
  tests/test_agent_forest_projection.py \
  tests/test_agent_durable_checkpoint.py \
  tests/test_agent_runtime_final_repair_research.py \
  tests/test_agent_memory_final.py \
  tests/test_agent_memory_durable.py \
  tests/test_agent_automation_final.py \
  tests/test_agent_final_system_scenarios.py \
  tests/test_agent_provider_matrix_chaos.py \
  tests/test_durable_agent_runtime.py \
  tests/test_agent_runtime_api.py \
  tests/test_agent_dock_state.py
uv run pytest -q tests/e2e/test_agent_dock_browser.py
uv run pytest -q
```

前端語法：

```bash
find src/stock_ai/ui/static/js -name '*.js' -exec node --check {} \;
```

2026-08-26 推進 GOV-004／GOV-005／GOV-006（維持 partial）：正式 `build_engine()` 現在建立 runtime-owned governance bundle，讓 approved-artifact rollback、change-set binding 與 content-addressed retention 使用同一個 migrated SQLite authority；`/api/system/capability-status` 回報實際 durable wiring，並由 restart／first-boot／`:memory:` fail-closed 測試覆蓋。所有策略／模型部署 writer、所有 order writer、durable scheduler 與 off-host archive 尚未全部接線或完成外部驗收，故不提前改為 complete。
2026-08-26 再補強 GOV-004／GOV-005（維持 partial）：paper-training promotion API 現在以 runtime-owned rollback registry 與策略版本寫入同一個 SQLite transaction，promotion 會同時留下 approved-artifact／activation receipt，重啟後可恢復 active artifact；runtime `TradeStore` 也會將每筆 PaperOMS 成交結果保存為同一 retention authority 的 immutable critical execution snapshot，重複 order ID 只重用原 snapshot。其他模型部署、手動 broker order writer、durable scheduler 與 off-host archive 尚未全部接線或完成外部驗收，故仍維持 partial。
2026-08-26 補強 GOV-004（維持 partial）：`ResearchModelRegistry.promote_champion()` 現在要求非 Agent 的人工核准者，並將 FinRL／Qlib champion model 的 artifact hash、promotion metadata 與 activation 透過同一 SQLite transaction 寫入 runtime-owned rollback registry；重啟後可恢復目前 champion。其餘模型 deployment writer、真實 production rollback drill 與外部驗收仍未完成，故不提前改為 complete。
2026-08-26 補強 GOV-005：`PaperBrokerSimulator` 現在可注入與 OMS 相同 SQLite 的 `ContentAddressedRetentionLedger`，每個 submit／fill／cancel／replace／expiry lifecycle projection 都會以 `stock_ai.paper_broker_execution_snapshot.v1` 保存 immutable critical snapshot；重啟後可驗證並重建 open→filled 歷史。durable scheduler、off-host archive、runtime wiring 與其他 execution-critical writers 尚待接線或外部驗收，故 GOV-005 維持 partial。
2026-08-26 再補強 GOV-005：正式 `/api/open-stock-ai/agent/paper-training/*` 的 `_broker()` factory 現在明確注入 runtime-owned retention authority，不再由 API 寫入路徑遺漏 broker lifecycle snapshot；factory／submit／restart 測試證明 API 使用同一 migrated SQLite ledger。durable scheduler、off-host archive 與其他 execution-critical writers 尚待接線或外部驗收，故仍維持 partial。

2026-09-07 完成 SEC-003／SEC-004：GitHub hosted workflow run `34083835719` 對 commit `41cab35851163ac575a10ea4b9308d59fb3d8de5` 執行第一方 CodeQL、Bandit 與從 `uv.lock` 匯出的完整釘定依賴稽核。CodeQL SARIF gate 檢查 45 個結果，security severity 7.0 以上為 0；Bandit high gate 與 pip-audit 均通過，pip-audit 檢查 53 個依賴且漏洞為 0。SARIF、hash receipt、pip-audit JSON、依賴清單與 SBOM 均由 GitHub artifact 保存。SEC-005 仍需簽名 release SBOM，不因本次一般 workflow artifact 提前完成。

2026-09-07 完成 R-009：`Hosted API load regression` workflow run `34104172227` 對 commit `9cc62cac0801fad307150d534b47bbad57bb54a5` 啟動隔離的真實 Stock AI API，baseline／candidate 各取得 300 筆樣本，錯誤率均為 0，candidate p95 為 2.815 ms、p99 為 3.019 ms、吞吐量為 20.000 req/s。gate 以版本化的 request rate、concurrency、duration、p95/p99、error-rate 與 throughput 門檻逐項比較，缺樣本、服務 identity／commit 不符、回歸或收據竄改都會 fail closed。GitHub artifact `10011715225` 保存 profile、content-addressed baseline、candidate report 與總 gate receipt 30 天；下載後重新驗證的 receipt SHA-256 為 `8df8787695a43995695072634f7ad99a7fdbbcd0c2e955672e11ee33e5d6cbcb`。

2026-09-07 完成 R-010：`Hosted chaos recovery` workflow run `34105444323` 在 GitHub Linux runner 對 commit `857472882735566510ecafae9e8ec77a16780624` 實際執行 SIGKILL、iptables OUTPUT REJECT、1 MiB tmpfs 寫滿至 ENOSPC，以及逾時子程序終止。四個 scenario 均重新開啟 durable SQLite state，確認新委託維持阻擋並寫入 operator receipt；兩個 SQLite artifact 的 `PRAGMA quick_check` 均為 `ok`。下載後獨立驗證 `open_stock_ai.hosted_chaos_gate_receipt.v1`，結果 `passed=true`、無 blocker，receipt SHA-256 為 `05a939fb36e4020062bcce24bae7cb54979d9a49b02bec98558b8f587f26f903`；GitHub artifact `10012175884` 保留到 2026-10-07。

2026-09-07 完成 R-011：`Hosted HA failover` workflow run `34106748368` 對 commit `54581d26021a4f51a4cc9b237e9ab6608e9f0142`，分別以獨立 primary／standby OS process 執行 API、Agent runtime、市場資料、券商閘道與排程器五個服務的 leader lease。每個 standby 都先被存活租約拒絕；primary 遭 SIGKILL 後，standby 於 0.516–1.017 秒內以 epoch 2 接替 epoch 1，舊 owner 的續租全部遭拒，重新開啟的 SQLite authority 與接管 epoch 一致。下載 artifact 後獨立驗證 `PRAGMA quick_check=ok` 與 `open_stock_ai.hosted_ha_failover_receipt.v1`，結果 `passed=true`、無 blocker，receipt SHA-256 為 `0e007e8a0649725667575b61dc89381edd16789de2fdb1cff7a29d5b78ce5ff5`；GitHub artifact `10012677079` 保留到 2026-10-07。

2026-09-07 完成 R-007：`Hosted circuit breaker recovery` workflow run `34107840277` 對 commit `ea303ae937201b97210d297268a5277dad492b04` 啟動獨立本機 HTTP upstream，分別向 provider、source 與 broker scope 送入連續三次真實 HTTP 503。三個 circuit 均依序 closed、closed、open；open 期間的額外請求在網路呼叫前被阻擋，伺服器 request count 維持 3，其他 scope 仍可獨立 admission。backoff 後唯一 half-open probe 取得 HTTP 200 並回到 closed，每個 scope 最終 request count 為 4。下載後獨立驗證所有 nested decision SHA-256 與總收據，結果 `passed=true`、無 blocker，receipt SHA-256 為 `15c4ee7944826ade48ffaa56ac8fac0bd0af794df5d6c4da4714e4d1277e4487`；GitHub artifact `10013098361` 保留到 2026-10-07。

2026-09-07 完成 R-008：`Hosted rate limit policy` workflow run `34108758767` 對 commit `baa52fced2017c0d286fc20499b62d8091c11aee` 啟動獨立 HTTP upstream，對 endpoint、tool、primary provider 與 alternate provider 四個 scope 實際回傳 429 與 `Retry-After`。每個 scope 的 hash-verified policy 固定 0.25 秒內 2 次、最大 concurrency 1；實測均先阻擋並行請求，再阻擋 backoff，恢復後准許兩次 HTTP 200 並以 verified window 阻擋第三次。未登錄 policy 的 tool scope 另以 conservative interval 阻擋過密請求後恢復。下載後驗證所有 policy／decision receipt 與總收據，結果 `passed=true`、無 blocker，receipt SHA-256 為 `b5e5f655470ef4f462972667ff28bf77f16516be0758e2559dee62cc070c5f31`；GitHub artifact `10013470557` 保留到 2026-10-07。

2026-09-07 完成 GOV-004：`Hosted artifact rollback drill` workflow 第一次 run `34109725703` 因未攜帶 process-local runtime session 被真實 API 以 403 拒絕，未被算成成功；修正後 run `34110052083` 對 commit `11f52014209f7a5a6ddf2046ca5f4ef0513cfafd` 從首頁取得 session 並經同源 header 呼叫正式 governance API。策略與模型 lane 分別從 content-addressed v2 回滾至 v1，另一 lane 不受影響，服務停止後重新開啟 SQLite 仍為 v1。相同 artifact ID 改寫 hash、啟用未批准 artifact 與 `approved_by=agent` 的 rollback 均遭拒；`PRAGMA quick_check=ok`。總收據 `passed=true`、無 blocker，receipt SHA-256 為 `f3c6f3da2c400735749397e10d7464fa3de47265afd86dd41324e30426c928f5`；GitHub artifact `10013972568` 保留到 2026-10-07。

## macOS `.command` 實機 UI 驗收

必須從本專案目錄執行，而不是啟動其他 clone：

```text
/Users/your-user/Desktop/AI股市系統-Agent測試版/開啟股市AI系統.command
```

啟動後依序保留可稽核證據：

1. 確認啟動器顯示的專案根目錄正是上述路徑，健康檢查成功，並打開實際選用的 loopback port。
2. 建立新 Session，執行普通問答 P88；確認只有必要 Research、沒有 Automation／不必要反問。
3. 同一 Session 執行 P89 複雜持股任務；觀察 Task Forest 多 Branch、Branch 內順序、Branch 間重疊執行、Host tool events 與 Join。
4. 在 running 狀態輸入 P94 新需求與 P16 股票代號修正；確認 Composer 始終可用，只有受影響 Branch 改變。
5. 觸發 P90 Decision Card；測推薦選項、替代選項、自由文字，確認由同一 `session_id`／`run_id` checkpoint 續跑。
6. 點擊 Task／Evidence／Artifact／Automation 節點，確認 context chip 含精確 artifact version／path；執行 revise、stale conflict、compare、restore。
7. 執行 Automation preview；未確認前不得 activate，確認後檢查 compile／validate／dry-run／active Host events。外部 n8n／通知僅在有測試憑證與權限的環境驗收。
8. 關閉 UI、停止服務後重新雙擊 `.command`；確認 Session、Forest、Decision、Artifact versions、Automation 與 events 由 SQLite 恢復且不重複執行。
9. 切換可用 provider 後執行同一模型矩陣；沒有實際可用的 provider 類別一律記為「需驗收」，不可用 mock 代替。
10. 逐項執行 P104 Chaos 注入並記錄 P105 KPI；真實交易始終保持停用，只做風控／批准拒絕的負向驗收。

## 驗收記錄規則

每次驗收至少記錄：branch／commit、時間、macOS 版本、`.command` 顯示的 project root／port、provider／model、scenario、輸入、Session／Run／Branch ID、Host event sequence、畫面截圖、測試命令與完整結果。任何一項尚未執行、被 skip／xfail、依賴不可用或只由 mock 覆蓋，都必須維持「需驗收」，不得改寫成「已完成」。
